# BRIEF — KPTA/Mesken Konsolidasyon Paketi

**Uygulayıcı:** Claude Opus 4.8 (Claude Code oturumu)
**Hazırlayan:** Fable 5 denetim oturumu, 2026-07-02
**Kaynak:** 4-ajanlı kod denetimi (motor, UI, render hattı, ML) + `Claude_instructions/DEVIR-NOTU.md` + ana repo `mesken/DURUM.md` + vektör-kernel yol haritası (`~/.claude/plans/50cm-lik-zgaralar-projelerin-daha-federated-adleman.md`)

---

## DURUM — İLERLEME KAYDI (Opus 4.8 oturumu, 2026-07-02)

**Nerede çalışılıyor:** ANA REPO (`/Users/ofbakirci/apps_ofb/katplanitasarim`). Worktree
(`.claude/worktrees/mystifying-noyce-557642`) gitignored asset'lerden (`input/`, `ref/`, `mesken/`
1.4G) YOKSUN → orada `npm test` tam yeşil olmaz ve pre-commit hook build'i patlar (mesken asset yok).
Tüm iş ana repoda yürüyor.

**Geri dönüş noktası (rollback):** `git tag konsolidasyon-pre-A-20260702` @ `488b70d` (Paket A öncesi temiz master).

| Adım | Durum | Commit | Not |
|---|---|---|---|
| **A1** snapshot-regresyon | BITTI | `9303b98` | `tests/snapshot-regression.js` + `tests/fixtures/snapshot-baseline.json`; 7 senaryo; strict suite'te; determinist BİREBİR. Doğrulama: `node tests/snapshot-regression.js` → sıfır fark. Baseline tazeleme (bilinçli): `--write`. |
| **A3** sessiz vazgeçme uyarıları | BITTI | `4194dca` | repairUnits(30)/rectifyCorridor(24)/slimUnitAntre(240) limit dolunca `console.warn`. A1 birebir korundu. **BULGU:** 7 baseline senaryosunda yalnız **rectifyCorridor 1 kez** limit doldurur; repairUnits & slimUnitAntre HİÇ dolmaz → onarım zinciri çoğunlukla yakınsıyor, tek riskli yol koridor-israfı aktarımı. |
| **A2** sihirli sayı → REG | BITTI | `39e6542` | `core.js REG`'e `iter`/`cap`/`layout` alt-bölümleri; haritadaki TÜM occurrence'lar taşındı (planner.js 14 nokta + rooms.js 2). Saf taşıma; her adımda A1 birebir + `npm test` 31/31. **DÜZELTME:** rectifyCorridorEnds `pass<24` DEĞİL `pass<8` kullanıyormuş (kaskad boşaltma turu) → `REG.iter.corridorEndDrain:8` olarak taşındı, 24'e bağlanMAdı. Aynı-değer/farklı-anlam ayrıştırıldı: 140 → `sigMaxAlan` + `ebBanyoBuyuk` (bağımsız). checks.js:177'deki 45 (salon-şişme info sinyali) farklı semantik + checks.js sabitleri A7 kapsamı → dokunulmadı. |
| **A4** alan tek-kaynak | BITTI | `7465a07` | `core.js areaOfCells(cells)` tek formül kaynağı; `walls.js calcRegionMetrics`'in `g.area` üretimi de artık ona bağlı. **24 çağrı noktası taşındı** (io.js 1, planner.js 15 satır/16 çağrı, walls.js 1, rooms.js 7). Disiplin: mutasyon-halindeki dizilerde (repairUnits/purgeSlivers/slimUnitAntre döngüsü) `g.area` BAYAT → `areaOfCells`; canlı+taze bölge metriği (sağ-tık menü alan gösterimi) → `g.area`. **Dokunulmadı (3 nokta, bilinçli):** `planner.js:522` `(restN-lob.length)*M*M` = hücre-SAYISI aritmetiği, cells dizisi değil; `app.js:203` + `app.js:214` = villaFloors PASİF-kat snapshot yolu — A1 tüm senaryolarda `villaFloors=null` → test-doğrulanmış birebirlik ALINAMIYOR, "şüphede davranışı koru" gereği bırakıldı (cebirsel özdeş, ileride villaFloors fixture'ıyla taşınabilir). `tests/*` kapsam dışı. Doğrulama: her dilim sonrası snapshot 7/7 BİREBİR + io export `area_m2` 38 alan byte-diff birebir + npm test 31/31 + build OK. |
| **A5** perf enstrümantasyon | BITTI | `0600dd1` | `core.js PROF` + `KPTA_PROFILE` bayrağı (varsayılan KAPALI, kapalıyken snapshot-regression 7/7 BİREBİR). `generate()` fazları: inside/cekirdek/yerlesim/layoutUnit-toplamı + 11 onarım geçişi AYRI AYRI + runChecks + render. `performance` guard'lı. `tests/perf-smoke.js` (diagnostics, strict DIŞI). **BULGU (ilk perf haritası):** süre **onarım zincirinde** yoğun, layout'ta DEĞİL. 48×27 derin blok ~5,0 s: `slimAntres` ~2,44 s (%49) + `rectifyCorridor` ~1,66 s (%33) + `rectifyRoomShape` ~0,81 s (%16) = ~%98; yerlesim 18 ms + layoutUnit 6 ms + çekirdek/inside <3 ms = ihmal. 32×16 standart ~0,58 s: slimAntres ~0,24 s + rectifyRoomShape ~0,25 s + rectifyCorridor ~0,05 s. Optimizasyon hedefi net: slimAntres/rectifyCorridor/rectifyRoomShape iç döngüleri (B/L1 sonrası). |
| **A6** CI + node --check | BITTI | `24f997c` | `.github/workflows/test.yml` YENİLENDİ (eskiden Node 24 minimal): push+PR, ubuntu-latest, Node 20, `npm install` (lockfile yok → `npm ci` değil). Adımlar: kök `.js` başına `node --check` (açık-if-bloğu regresyon otomasyonu) + `test:syntax` + `npm test` (31) + `test:smoke` + `node tools/bundle.js`. **NOT (build neden kısmi):** CI'da `npm run build` KOŞMAZ → yalnız `node tools/bundle.js`. `npm run build` postbuild'i (`mesken/build-prototip.js`) tetikler; o gitignored `mesken/` asset'lerine (02_PROTOTIP/prototip.template.html) muhtaç, runner'da yok → patlar. Bundle tek başına yalnız tracked kök kaynakları kullanır → yeşil. Repo PUBLIC: workflow'a sır/path yazılmadı, mesken asset'i eklenmedi. **Actions ilk koşusu ANCAK push sonrası doğrulanır** (push kararı kullanıcının). |
| **A7** checks.js kural-tablosu | BITTI (2026-07-03) | `729eae3`+`654cf34`+`bfdf480`+`e19e1bc` | `collectChecks` monolit → **9 kayıtlı kural** (`CHECK_RULES`, KARARLI ASCII id: IMPORT_REPAIR / UNIT_ROOMS / DOORS / FLOOR / CORE / PARSEL / AVLU / SITE / BALKON); her `run(add,p)`, karmaşık toplayıcılar tek kural=çok satır (aşırı soyutlama yok). Her çıktı satırı `{id,s,t,reg,unit,action}` — id daima registry kümesinde (`checkRuleIds()`). UNIT_ROOMS piyes satırları **14 ince subId** (SALON_MIN/YATAK_MIN/YATAK_SAYI/EBEVEYN_BANYO/BANYO_YOK/MUTFAK_YOK/SALON_YOK/MUTFAK_MIN/BANYO_MIN/WC_MIN/DOGAL_ISIK/DAIRE_SISME/BICIM/ERISIM) = ML mevzuat filtresi/DXF tüketicisi için. Registry `window.CHECK_RULES`/`checkRuleIds` ile açık. **A2 devri kapatıldı:** checks.js sihirli sabitleri (şişme 22/13/23/5×1.4, salon-payı 45/0.5, biçim 0.55, ort-daire 30/15/25/6×1.6, villa hiza 0.26) → `REG.checks` (core.js, DEĞER BİREBİR). **Hakem:** `tests/checks-metin.js` (7 senaryo collectChecks tam çıktısı, byte-frozen) + `tests/checks-registry.js` (sözleşme). **Dokunulmadı/taşınamadı:** FLOOR/CORE/PARSEL/AVLU/SITE/BALKON bilinçli tek-kural (satır-içi interleave/order zorunluluğu → ayrı kurala bölünemez); `renderChecks`/`runChecks`/B7 action-meta AYNEN; helper fonksiyonlar (collectUsage/parkingSummary/collectCoreDim/Height/GroundEntrance) imza-uyumlu, dokunulmadı. Kapılar: npm test **35 dosya** + snapshot 7/7 BİREBİR + build OK. |
| **B1** cut-drag hayalet önizleme | BITTI | `77b44ea` | Drag SIRASINDA `generate` çağrısı SIFIR (eskiden her mousemove `generate(true)` → 48×27'de ~5 sn/frame). Yeni: hafif `#dragOverlay` (kesikli ayırıcı + taşınan tutamaç + imleç yanı canlı ölçü "6,5 m \| 4,0 m"); reflow yalnız BIRAKINCA (`finishDrag`), mevcut commit yolu (generate + `restoreEditedFootprints(preUnits)` + cut undo/redo) BİREBİR korundu. Mavi duvar-node: komşu iki bölgenin canlı W×H etiketi. **KARAR:** commit ertelemesi `setTimeout(0)` (rAF DEĞİL — arka-plan/odaksız sekmede rAF durur → commit asılı kalırdı; tarayıcıda kanıtlandı). Dosyalar: render.js/interaction.js/structure.js. Doğrulama (tarayıcı, gerçek olay): drag'de generate=0, drop tek generate, undo→[[16..]]/redo→[[7..]] birebir, overlay drop'ta temizlenir. **BULGU (drop-commit süresi):** standart 32×16 (26 bölge) planda drop→yeni-yerleşim GÖZE ANINDA (progress imleci görünmüyor bile); ~5 sn'lik his ANCAK 48×27 derin blokta beklenir (A5 haritası) — ileriki onarım-zinciri (slimAntres %49 + rectifyCorridor %33) optimizasyonu B1'i daha da akıcı yapar ama B1 zaten frame-donmasını kaldırdı. snapshot 7/7 birebir. |
| **B5** stepper debounce | BITTI | `eaeeb06` | `debSafeGen` (200 ms trailing) — spec input basılı-tutta her tetikte `safeGen` yerine son değişimden 200 ms sonra TEK üretim. Bağlı: daire spec change + katSayisi/katYuk + bodrumSayisi (yapısal `onFloorCountChange` senkron kalır, yalnız generate debounce'lu); addUnit/binaTipi senkron. Tarayıcı: 6 hızlı change → 0 anlık + 200 ms sonra 1 generate. app.js. snapshot 7/7 birebir. |
| **B2** mod rozeti | BITTI | `b9c3954` | `#modeBadge` (sol-üst, toolbar hizası) — `setMode`→`updateModeBadge`: aktif mod Türkçe adı + tek satır ipucu + inline SVG mod ikonu (emoji YOK). draw/pan sade (gizli); park kendi çubuğunu gösterir; villa/site sekmesi açıkken `.shifted` (bir satır aşağı); mobil (<=700px) toolbar altına. Tarayıcı: 8 mod geçişi doğru, ekran görüntüsü OK. |
| **B3** klavye kısayolları | BITTI | `2fef4df` | `MODE_KEYS` D/O/K/B/A/Y/P/T/S → ilgili araç düğmesini `.click()` (pro-only/site/park görünürlüğü + tSite toggle OTOMATİK korunur). Guard: form alanı + `dragging` + Ctrl/Cmd/Alt YUTULUR; Space/Esc/Ctrl+Z/Y ayrı handler (dokunulmadı). Tooltip'lere "(X)". Tarayıcı: tüm tuşlar + 3 guard + Pro-mod P doğrulandı. **Kenar durum:** toolbar COLLAPSED iken (`.tbgrp{display:none}`) kısayollar da pasif (gizli düğme = kısayol yok) — bilinçli. |
| **B4** tutamaç affordance | BITTI | `fd70d2f` | Hover'da tutamaç büyür + hâle (cut/duvar/çekirdek/bina-sınır TÜM aileler) + durum çubuğu (`#stHint`) tek satır: turuncu cut / mavi duvar / mavi çekirdek / turuncu bina-sınır. Yapı modunda çekirdek tutamaçları MAVİ-dolgu kareler, bina sınır TURUNCU daireler = görsel aile ayrımı. `hoverCut`/`hoverStructH` global; render yalnız hover DEĞİŞİNCE (hafif). Tarayıcı: 3 aile + ekran görüntüsü doğrulandı. Mobil: `#status` gizli → dokunmatiği bozmaz (touch.js yeşil). |
| **B6** parsel kapanış | BITTI | `b7d41ca` | Kapanınca kontur kesikliden DÜZE (`render.js` stroke-dasharray koşullu) + kalıcı durum çubuğu ipucu. **BULGU/DÜZELTME:** brief "kenara SAĞ tıkla" diyordu ama ön-cephe seçimi LEFT-click (`interaction.js` `e.button!==0 return`) → ipucu gerçek davranışa göre "bir kenara tıklayın" yazıldı. Tarayıcı: dashed→solid + yeşil ipucu OK. |
| **B7** denetim→eylem köprüsü | BITTI | `9bb61f4` | Piyes-ölçüsü bad satırlarına (salon/salon+mutfak, yatak, mutfak, banyo, wc, eb.banyo) `action` meta + panelde "Öneri" düğmesi → `focusRegion(oda)` + durum çubuğunda öneri. OTOMATİK DÜZELTME YOK. Denetim mantığı/metni BİREBİR: **snapshot 7/7 birebir (bad/info sayıları değişmedi)**. Düğme yalnız reg'li bad satırlarda; "yerleştirilemedi"/biçimsiz/ok satırlarında YOK. Tarayıcı: 4 düğme + odak + turuncu öneri + ekran görüntüsü doğrulandı. |
| **B TAMAM** → yol ayrımı | BEKLIYOR | — | Paket B bitti. Sıradaki: L1-A (kalınlık/brüt-net/DXF, ayrı plan belgesi) ya da A7 (checks.js kural-tablosu) ya da Paket C (render sertleştirme) — **kullanıcı seçer**. |
| **C2** snapshot çözünürlük kilidi | BITTI | `f216a3c` | `view3d.js snapCameraDataURL` viewport'tan BAĞIMSIZ sabit **1440×810** PNG (setPixelRatio(1)+setSize(1440,810,false)+aspect=16/9; try/finally BİREBİR geri). **Tarayıcı doğrulaması (gerçek WebGL):** 660px & 1280px pencerede snapshot AYNI 1440×810 (dpr=2'de bile); exportCameras px_delta before==after (worldToPx px_delta=0 korundu); renderer restore OK; adım-4 iki-tık yerleştirme+export(room_id)+snapshot+sil çalışıyor; 0×0 guard. Kanıt: scratchpad `C2-locked-snapshot-1440x810.jpg`. |
| **C3** oda tipi fallback uyarısı | BITTI | `07e6015` | `io.js buildFloorplanMap` tanınmayan tip (`fpRoomEnum→'room'→_def` bej) oranı >%5 ise `console.warn`+export `warnings:[]`. Yeni `fpTypeRecognized()` fpRoomEnum tanıma mantığını aynalar. **Doğrulama:** normal plan (38 bölge)→`warnings []` (sessiz); bozuk plan (6/38=%15.8)→uyarı. npm test 31/31, gen-floorplan-map yeşil, snapshot 7/7 birebir. |
| **B3 revizyon** collapsed kısayol | BITTI | `a3e0eb3` | Kullanıcı kararı: collapsed toolbar'da kısayol AKTİF. **BULGU:** zaten aktifti — önceki "pasif" notu yanlış zihinsel modele dayanıyordu (`getComputedStyle(btn).display` düğmenin KENDİ display'ini döndürür; collapse ata `.tbgrp`'yi gizler, düğme 'flex' kalır). Guard'ı kaldırmak pro-only/site/park'ı bozardı → kaldırılmadı, yorum düzeltildi. **Tarayıcı:** collapsed 'o'→tetiklendi, pro-only 'p'→tetiklenmedi. |
| **C1** model sürümü + seed | BITTI | gitignored → `DURUM.md §8` | `render-server.js` (yedek `.bak-20260702`) startup'ta sürümü çözer+pinler (ÜCRETSİZ GET, KREDİ YAKMAZ)+/health'e yazar+güvenli geri-düşüş. **BULGU:** sürüm=`712e06a8e122…fd42f374` (versiyonlu, iki koşu DETERMİNİST); nano-banana-pro **seed KABUL ETMİYOR** (`acceptsSeed:false`)→seed enjekte edilmedi. **Paid smoke render YAPILMADI** (kullanıcı kredi kısıtı); sürüm ücretsiz metadata GET ile doğrulandı. Hard-pin: `NANO_VERSION` env. |
| **C4** render regresyon fikstürü | BITTI | gitignored → `DURUM.md §8` | `mesken/tests_render/` (inputs test7-9 + reference + `check.py`). check.py render-server'ı sürücü kullanır→pinli sürüm+isoPrompt TEK KAYNAK. **Doğrulama:** `--dry` (kredi yakmaz) 3 kıyas görseli üretti (girdi\|referans). Canlı koşu (3 nano=kredi) kullanıcının. |
| **C5** mobilya render sinyali | BITTI (paralı adım 5 dahil) | tracked `view3d.js`(C6 ile) + gitignored `iso_render.py`/`render-server.js` → `DURUM.md §8` | Tarayıcı iso PNG davranışı belgelendi (tutarlı, düzeltme yok). **Headless parity:** `iso_render.py` furniture[]'tan sade kutu çizer (FURN_DIM port + furn_footprint, koyu kategori tonu, `__w/__d`, mobilyasız=bayt-özdeş). **KANIT:** 126-item furnished plan → `tests_render/out/C5_parity_kiyas.png`; **sayısal parite 0 px**. **Prompt:** isoPrompt'a "furniture blocks are real positions, dress in place" koşullu eklendi. **Paralı A/B (3 render, onaylı):** `C5_AB_prompt_kiyas.png` — HÜKÜM: txt2img iso'da yeni prompt güvenli/nötr (renk-kutuları soyut, konum sıkı kilitlenmiyor); ASIL sadakat B/img2img yolunda (C7_B mobilyayı YERİNDE korudu). |
| **C6** depth map export (STRETCH) | KOD TAMAM, canlı yakalama preview-kısıtlı | `view3d.js snapCameraDepthMap` → `DURUM.md §8` | `snapCameraDepthMap(cam)` eklendi + API'ye açıldı: C2 disiplini (1440×810/pixelRatio1/try-finally) + `MeshDepthMaterial` overrideMaterial + near/far `scene.__span`'e göre. Pipeline'a BAĞLANMADI (kaçış yolu). Canlı depth PNG YAKALANAMADI: preview penceresi 0×0/hidden (ekran-arası tuzak) → renderer 0×0 → guard null döner (doğru). Parse-temiz, snapCameraDataURL ikizi. |
| **C7** A/B varsayılanı | BITTI (2026-07-03) | tracked `view3d.js` (`camRenderMethod='snapshot'`) + gitignored `render-server.js`/`prototip.template.html` → `DURUM.md §8` | **B (snapshot/img2img) VARSAYILAN, A/İkisi istek-üzerine** (2×→1× maliyet). `render-server runCameraRecipe` default `'both'`→`'snapshot'` (+snapshot yoksa A'ya güvenli düşüş); prototip 3 fallback + kopya güncel; view3d `camRenderMethod` default flip. Seçici düğmeler (Prompt/Görsel/İkisi) + setCamRenderMethod DEĞİŞMEDİ → A/both hâlâ seçilebilir. Kamera YERLEŞTİRME UI'ına dokunulmadı. Tarayıcı: `exportCameras`→`render_method:'snapshot'` doğrulandı. **Paralı doğrulama (onaylı):** `POST /camera` method'suz → server log `method=snapshot`, yalnız B-submit (A yok), `a=false b=true` → `tests_render/out/C7_B.png`. |
| **KAT-52** küçük-kat band-koridor israfı (K1+K2+K3) | KULLANICI ONAYI BEKLIYOR (2026-07-04, Opus 4.8 uygulayıcı) | `674c82f`(K1)+`95d65ee`(K2)+`16a66e2`(K3)+`d742ad6`(test) · rollback tag `motor-pre-kat52-20260704` | Davranış-değiştiren MOTOR işi; 3 dar-kapılı iş kalemi, hepsi mevcut sağlıklı planlarda NO-OP. **VAKA (kat-plani-52, ~140 m² eğik 4-köşe, 1+1 ×2 adet):** ÖNCE tam-en band koridor karşı kanadı sterilize edip ORTAK DEPO'ya düşürüyordu → daireler **[31,5 / 29,75] m²**, APARTMAN HOLÜ 26 m² + 2× ORTAK DEPO 31,5 m² = **kat %41 ölü**, koridor uç boğumu, **5 bad**. **K1 (`trySmallFloorCore`, planner.js + `REG.layout.kucukKatAlan:180`):** kapı `perFloor==2 && taban≤180`; kompakt çekirdek üst-sol köşe + koridor hemen altında tam-boy → DAİRE 1 üst kanat (side'S'), DAİRE 2 alt kanat (side'N'), her ikisi koridora tek temiz kenardan bakar (SARMA YOK → giriş kapısı garanti); sığmazsa band'a geri düş. **K2 (`assignOrphanRooms`, fixOrphans yakını):** unit'siz TİPLİ oda → en uzun ortak-duvarlı komşu daireye kat / erit; ORTAK DEPO/çekirdek/koridor DOKUNULMAZ; bu vakada zaten yok (güvenlik ağı). **K3 (`rectifyCorridorPinch`, rectifyCorridorEnds sonrası):** koridor UÇ boğumunu (corridorMinWidth<koridorMin) drainable ise komşuya devret; regConnected/DELTA/healDisconnected mühürleri AYNEN. **SONRA:** daireler **[36,5 / 62,75] m²** (büyüdü, toplam 61→99), **ORTAK DEPO YOK**, **giriş kapıları 2/2 yerleşti**, **3 bad**. **AÇIK KALAN (dogma değil, dürüstçe raporlanır):** koridor minW=1 m (çekirdeğe dayalı boğum, tek komşu struct → K3 drain edemez), koridor %14, 2 "biçimsiz yatak" (8 m sığ taban kanat derinliğini sınırlar). **BASELINE PROTOKOLÜ:** 7 mevcut snapshot senaryosu BİREBİR korundu (K1/K2/K3 kapıları hiçbirine girmez — doğrulandı `--write` öncesi 7/7 OK); YENİ 8. senaryo `kat52-kucuk` eklendi (bölge=13 daire=[36,5/62,75] kapı=10 bad=3). **Kapılar:** npm test **38 dosya** (+`tests/kucuk-kat.js`) yeşil + snapshot **8/8** BİREBİR + build OK. **GÖRSEL KANIT:** ÖNCE/SONRA plan SVG kıyası tarayıcıda üretildi (band+2×depo → kompakt çekirdek+çift-yüklü koridor+2 tam daire). Kullanıcı bununla onaylayacak (C5-R dersi: son kapı KULLANICI). |
| **EGIK-DUVAR** eğik dış duvarlı planlarda 3 sorun (E1+E2+E3) | KULLANICI ONAYI BEKLIYOR (2026-07-04, Opus 4.8 uygulayıcı) | tracked `walls.js`+`checks.js`+`rooms.js`+`interaction.js`+`tests/*` · PUSH EDİLMEDİ · rollback tag YOK (temiz master üstünde, `git stash`/`checkout` yeter) | 90°'ye kilitli OLMAYAN (eğik) dış duvarlı planların ARA-DÖNEM mitigasyonu (gerçek diyagonal = L1-B vektör işi, YAPILMADI). **E1 SAHTE "koridor 1 m" bad (kök neden bulundu):** kat-plani-54/55 = kat-52 ile AYNI eğik 4-köşe taban (sol dış duvar 0,4 m eğik). Koridorun UÇ sütunu (c0) eğik cephe basamağı bir hücreyi tıraşladığı için 2 hücreye (1 m) düşüyor; ince ucun ÖTESİ bina DIŞI (`belowOut=true`) → SAHTE boğum, işlevsel en tüm boyunca 1,5 m. FIX: `corridorMinWidth(g,cols,inside,rows)` opsiyonel `inside` alır; dik-kesitin ince ucu bina dışına (cephe basamağı) dayanan hizalar minimum hesabından ATLANIR (iç boğum — her iki uç iç bölge — AYNEN sayılır). `inside` YOKSA davranış BİREBİR eski (sentetik bant birim testleri korunur). **Yalnız `checks.js` (kullanıcı-görünür uyarı)** inside-aware çağırır; `planner.js:1859` rectifyCorridor guard ve K3 `rectifyCorridorPinch` **BİLEREK ham** bırakıldı (inside-aware verince 7 ortogonal snapshot L-shape+deep-48x27 SAPTI → içsel generate-zamanı fonksiyonlarında over-blocking GÜVENLİ, dokunulmadı). **ÖNCE/SONRA (stored kat-54 import):** koridor denetimi `bad "1 m < 1,2 m — koridor daralmış"` → `ok "1,5 m ≥ 1,2 m"`; toplam bad **6→5**. Motor generate(kat-52) bad **3→2**. **K3 ODA-YEME KONTROLÜ:** K3 (değiştirilMEDİ) bu sahte boğumda oda YEMİYOR — kendi bad-sayısı guard'ı (`b>badRef→ok=false`) draini reddediyor (koridor 79 hücre sabit, doğrulandı). **E2 hol genişletme:** wall-drag (`moveWallStep`) koridoru büyütüyor (79→88, doğrulandı), "Holü sağa/aşağı uzat" (`corridorExtendStep(1)`) +3 hücre; "sola/yukarı" bina KENARINDA haklı olarak `false` döner AMA menü `doExt→failC` ile "Sola/yukarı uzatılamadı" NOTU gösterir (sessiz değil). "Holü çekirdeğe uzat" bu planda no-op (çekirdek zaten komşu). Kök neden: E2 asıl olarak E1'in temizlenemeyen sahte bad'iydi → E1 fix'iyle çözülür. **E3 hayalet balkon (kök neden):** balkon geometrisi eğik kenarda DEJENERE DEĞİL (ghostBalk ei=3'te 4,5 m² sağlam dörtgen üretir, hitBalk bulur) — asıl bug: silme yalnız `mode==='balkon'` sağ-tık dalındaydı → mod değişince silinemeyen balkon. FIX (a) `rooms.js` contextmenu handler EN ÜSTÜNE (door/avlu erken-return'lerinden ÖNCE) hitBalk kontrolü: balkon modunda doğrudan siler, DİĞER modda "BALKON / Balkonu sil" mini menü (avlu deseni); (b) `interaction.js` yerleştirmede dejenere-span guard + eğik kenarda dürüst durum mesajı ("eğik dış duvara eklendi… paralel oturur") + ghostBalk null'da "eklenemedi" mesajı; (c) `interaction.js` balkon modunda Del/Backspace hover-silme emniyet ağı. **Kapılar:** npm test **39 dosya** (+`tests/egik-duvar.js`: geriye-uyum+eğik-farkında+motor+balkon-geometri) yeşil + snapshot **8/8** BİREBİR (kat52-kucuk bad 3→2 bilinçli `--write`, 7 ortogonal + checks-metin 7/7 dokunulmadı) + build OK. **TARAYICI (fresh scripts, cache-bust'lı geçici shell — TUZAK: `?cb` yalnız HTML'i büster, çocuk `.js` cached → ayrı cb'li shell şart):** stored kat-54 import → koridor "ok 1,5 m" bad=5; kat-55 door-modunda balkona sağ-tık → "Balkonu sil" menü → 1→0; balkon-modu Del-hover → 1→0; corridorExtendStep sağ=+3/sol=false. **MÜHÜRLER DOKUNULMADI:** regConnected DELTA, healDisconnected koridor, fixOrphans !isStructReg, salon-emici, K3 drain guard'ları AYNEN. **BİLİNEN SINIR (L1-B'ye):** eğik-farkındalık RASTER cephe-basamağı sezgiseli (inside-adjacency); gerçek pts-poligonuna dik geometrik mesafe DEĞİL — çok-basamaklı sığ diyagonalde hâlâ az-uyarabilir (güvenli yön). Diyagonal duvar/oda/balkon ilk-sınıf desteği vektör kernel işi. |
| **C5-R** isoPrompt geri alımı | BITTI (2026-07-03, Fable) | gitignored `render-server.js` → `DURUM.md §8 C5-R` | Kullanıcı C5 prompt cümlesinin çıktısını REDDETTİ (zeminler boyalı kaldı, mobilya tekdüze koyu-kahve — `tests_render/out/C5_AB_prompt_kiyas.png` sağ panel + `C7_B.png`). `isoPrompt` C5-öncesi kazanan cümleye BİREBİR döndü (`.bak-20260703` diff'iyle doğrulandı, `node --check` temiz). KALAN: C7 (B-varsayılan), `/render {promptOverride}` debug, `iso_render.py` mobilya çizimi (parity), `snapDataURL(furnished)` seçimi — hepsi geçerli. DERS: prompt'ta LOCK (geometri/konum) ile RESTYLE (renk/malzeme) ayrışmalı; "exactly in place" görünümü de donduruyor; konum sadakatinin yolu txt2img prompt'u değil B/img2img. Oturumun "nötr-artı" hükmü insan-gözüyle yanlışlandı — görsel işlerde son kapı KULLANICI ONAYIDIR. |
| **AVLU paketi** | BITTI (2026-07-03) | `a4bc24a`(AV-1)+`5083fbc`(AV-2)+`be74b1d`(AV-3) | 3 dilim, motor+UI+denetim. **AV-1** (undo/redo fix): kök neden = avlu `pushEdit` yazılıyordu ama `avluChanged->generate`'in geçmiş filtresi (planner.js 453+2193) taze `'avlu'` kaydını anında siliyordu → Geçmiş boş, undo/redo etkisiz. FIX: her iki filtreye `'avlu'` eklendi (kayıt dünya-koordinatlı `prev` taşır, cut gibi generate-sonrası geçerli); redo `__snap`/stateSnapshot simetrisiyle courtyards'ı kurar. **AV-2** (düzenleme UI): `hitAvluHandle` köşe(2-eksen)/kenar(1-eksen) boyut + gövde taşı (balkon deseni ruhu, bbox); `avluMove`/`avluResize` → `avluGhost` önizleme, geçersiz(sınır dışı)=KIRMIZI+bırakınca revert+durum mesajı, min 1×1 m, drag'de generate YOK (B1 ruhu) reflow bırakınca; render 8 tutamaç; sil keşfedilebilir (avlu modu sağ-tık AYNEN + mod rozeti ipucu; BAŞKA modda avluya sağ-tık→küçük "AVLU/Avluyu sil" menüsü, hücresiz bölge oda menüsü çıkarmaz). **AV-3** (koridor-bölme): GUARD `avluCommitGuard` = generate sonrası koridor `regComponentCount` DELTA arttıysa revert+"Avlu koridoru bölemez" (2→2 geçer, çekirdek-bölünmüş korunur; `healDisconnected` koridor kararı BOZULMADI); KIRMIZI DENETİM `avluSplitsCorridor` (avlu koridorun 2+ parçasına komşuysa böler, çekirdek-split yanlış-pozitif vermez) → AVLU registry kuralına bad+reg+action, id kararlı. **BULGU:** `avlu-blok.js` E) regex `/Avlu/`→`/kısa kenar/` daraltıldı — merkezi geniş test-avlusu koridoru GERÇEKTEN bölüyor (yeni doğru bad); boyut-testi niyeti korundu. **Kapılar:** npm test 36 dosya (avlu-edit=32 assert) + snapshot 7/7 + checks-metin 7/7 BİREBİR + build OK. Tarayıcı (gerçek mouse): resize 8→12m, move, invalid-red-revert, iki sil yolu, guard-reddi (courtyards geri/durum mesajı), zorlanmış-split panelde AVLU bad, normal köşe-avlu kabul. **KAPSAM DIŞI (süresiz ertelendi):** koridoru avlunun etrafından yeniden rotalama (guard varken gereksiz; gerekirse L1-B vektör zemininde). |
| **AVLU-R** devralma turu (Fable + Sonnet inceleme ajanı) | BITTI (2026-07-03) | `5db3417`+`346d635` | Push sonrası CI KIRMIZI çıktı → kök neden avlu DEĞİL, gitignored `mesken/inputs/master1.svg` fixture'ına korumasız bağımlı 2 test: `camera-prompt` (yoksa zarif atlama — mesken-özel) + `brut-alan` (yoksa üretilmiş 32×16 plana düşme — T1-T4 jenerik, CI kapsamı korunur; T4'ün "en büyük brüt-net farkı=dış cepheli" varsayımı düzeltildi → plan-agnostik "en az bir oda brütü artmalı"). Doğrulama yöntemi: **salt-tracked yerel klon = birebir CI simülasyonu** (`git clone --local` → npm test exit=0) — gelecekte fixture şüphesinde aynı yöntemi kullan. Bağımsız Sonnet inceleme ajanı AV-1/2/3'ü brief'e karşı denetledi: spesifikasyona uygun, fixture'lar el değmemiş; **1 gerçek bug buldu:** `mobile.js` touchstart grab kararı eski `hitAvlu` mantığında → dokunmatikte mevcut avlu taşı/boyutlandır ÖLÜ (sürükleme PAN'e düşüyordu). FIX: `hitAvluHandle` gövde/kenar/köşe → hemen sürükleme (masaüstü paritesi); silme dokunmatikte avlu-dışı mod uzun-basış mini menüsünden. Test: `touch.js` vaka 7 (16→19 assert). Bilinen küçük takas (önceden var, yeni değil): guard-reddi `generate()` çağırdığından elle kapı düzenlemelerini sıfırlar — generate sözleşmesi, kabul. Kapılar: 36 dosya + snapshot 7/7 + checks-metin 7/7 + build OK. |
| **VERI-MUSLUGU** self-training günlüğü | BITTI (2026-07-03) | `9743e84` | Paket D ön-adım: motor render'a giden/indirilen planlardan tercih verisi BUGÜNDEN yerelde birikir (ağ YOK). `core.js trainLog` modülü (localStorage `kptaTrainLog`, RING BUFFER MAX=20 kayıt + ~2,5 MB soft-limit, tek kayıt >1 MB ise state'siz özet=`stateOmitted`, localStorage yoksa sessiz no-op). Kayıt: `{t:ISO, ev:'accept', kind, edits:{n,byType}, spec:unitSpecs, state:stateSnapshot()}`. **Kabul kancaları (io.js):** SVG/PNG/DXF indir + mesken köprüsü `mskExportRenderInputs` (render). **DEBUG-HARİÇ kararı:** AI-boyama/controlnet-edges/duvar-sınırı PNG (`exportAIPaintPNG/EdgeMask/WallBoundary`) = render hattının ARA ÇIKTILARI/tanı görselleri → kanca YOK (AI Output düğmesi üçünü toplu üretir; ayrıca loglamak aynı planı defalarca yazıp sinyali kirletir; nihai render kabulü zaten mesken köprüsünden gelir). **UI:** sol panel dışa-aktarım altında `#trainSec` katlanır `<details>` (pro-only, `.wt-sec` deseni): kayıt sayısı + "JSONL indir" (`kpta-egitim-gunlugu.jsonl`) + "Temizle"; emoji YOK. **Test:** `tests/train-log.js` (23 assert: no-op guard / kayıt+alanlar / ring-buffer 25→20 en-eski-düşer / >1MB→stateOmitted / JSONL satır-başına-geçerli-JSON / clear); strict suite'e eklendi. **Motor mantığı DEĞİŞMEDİ** (planner/walls/rooms/checks el değmedi). Kapılar: npm test **37 dosya** yeşil + snapshot 7/7 BİREBİR + checks-metin 7/7 BİREBİR + build OK. Tarayıcı: preview stale-cache tuzağına düştü (in-memory eski io.js); TAZE-served core.js gerçek browser localStorage'ıyla eval edildi → record/ring/JSONL/clear doğrulandı. |
| **PENCERE** ilk-sınıf vatandaş (kapının ikizi) | KULLANICI ONAYI BEKLIYOR (2026-07-04, Opus 4.8 uygulayıcı) · PUSH EDİLMEDİ · rollback: temiz master üstünde (`git stash`/`checkout` yeter) | tracked: yeni `windows.js` + `core.js`/`app.js`/`planner.js`/`io.js`/`rooms.js`/`render.js`/`view3d.js`/`icons.js`/`interaction.js`/`styles.css`/`kat-plani-tasarim.html`/`tests/pencere.js`+`run-all.js` · gitignored: `render-server.js` (§8 + `.bak-20260704-pencere`) | Otomatik cephe-pencere işaretleri (eskiden yalnız render.js FAZ3 mavi çift-çizgi) → EDİTLENEBİLİR nesneler. **P1 VERİ MODELİ (`windows.js` = doors.js ikizi):** `computeWindows()` = otorite; auto set (yaşam-odası komşulu cephe parçaları, FAZ3 segmentleme AYNEN) + `windowOverrides`(taşı ei/t + boyut w/height/sill/full)/`extraWindows`/`windowHidden`; `generate()` SIFIRLAR (kapı sözleşmesi, planner.js 2 nokta + tüm reset noktaları); `stateSnapshot/restoreState` additive `windows:{ov,extra,hidden}` (eski kayıt → {}). Varsayılanlar `REG.pencere` (wDef1.4→genişlik, h1.4, sill0.9, tamBoy=sill0+H=duvar). **P2 2B ARAÇ:** toolbar `#tWin` (Kapı ikizi, inline SVG `window` ikon, EMOJİ YOK) + kısayol **W** (B3 `MODE_KEYS`, d/o/k/b/a/y/p/t/s ile çakışmaz); çift-tık ekle/sil, sürükle-taşı (cepheye snap `winEdgeNear`); seçili pencere `#winPanel` (sağ-alt): Genişlik+Yükseklik+Parapet slider + "Tam boy cam" checkbox (full→H/parapet disable). Genişlik 2B'de görünür; yükseklik/parapet 3B'yi etkiler. **P3 3B (view3d wallEdge ikizi):** pencere BOŞLUĞU tam-yükseklik duvarı oyar → parapet altı (`isWin`) + lento üstü (`lintels`, roof-off gizli) + boşlukta saydam CAM paneli (`matGlass` opacity .34) + ince kasa (`matFrame`); tam boyda parapet yok; `applyRoof` isWin scale. İso+kamera+PiP hepsinde (sahne mesh'i). **P4 EXPORT:** `buildFloorplanMap().windows[]` ADDITIVE (doors[] şeması ikizi: orient/width_m/height_m/sill_m/full + p0/p1 px+norm); mevcut alanlar/şema DEĞİŞMEZ. render-server isoPrompt +1 cümle ("Windows are real openings with glass; at night interior light glows through them", LOCK'a dokunmadan; 0-kredi grep kanıtı, `.bak`+DURUM §8). **SINIR:** checks DOGAL_ISIK cephe-komşuluğuna bakar (pencere nesnesine DEĞİL) → DOKUNULMADI; motor yerleşim/bölge/alan DEĞİŞMEZ; kapı sistemi bozulmadı. **Kapılar:** npm test **40 dosya** (+`tests/pencere.js` 19 assert: auto-set/override/ekle-sil/gizle/generate-reset/snapshot-roundtrip/export-şema+doors-intact) + snapshot **8/8** BİREBİR + checks-metin **7/7** BİREBİR + build OK. **TARAYICI (fresh scripts, `?cb` cache-bust'lı geçici shell — TUZAK Bölüm 7: py-cache çocuk `.js`'i cached tutar):** 2B ekle(16→17)/taşı(t değişti)/sil(→16)/genişlik(2.6m export'a yansıdı)/tam-boy(sill0/H2.7); snapshot→restore override+extra korundu; 3B açıldı → cephe openingleri + 1 tam-boy(h2.7/s0)+15 normal(h1.4/s0.9) `getMap` doğrulandı, konsol hatasız. |
| **3B-UX-A** gezinme + görüş kilidi (view3d.js) | KULLANICI ONAYI BEKLIYOR (2026-07-03, Opus 4.8 uygulayıcı) | `0277054`(A1)+`a26ee41`(A2)+`f4bf366`(A3+A4) | 4 iş kalemi, hepsi `view3d.js` (KPTA kaynağı → build prototipe iner). **A1 GEZİNME:** `attachOrbit` elle-sabitleri **NAV yerel ayar nesnesine** taşındı (REG'e DEĞİL — motor değil): `rotateSpeed 0.78→0.5` (sakin çevirme), `rotDamp 0.12→0.2` (kısa/taşmayan atalet), pan artık ayrı `panDamp:0.2`+`panSpeed:0.85` (eskiden pan dampingFactor'ü paylaşıyordu), `zoomDamp:0.16` korundu, `tweenMs:280`. +`controls.tweenTarget/cancelTween/getDistanceTarget/tweenToAngle` (A3/A4 altyapı). **A2 ZOOM HER YERDE:** `#v3dZoom` 'Görünüm' çekmecesinden çıktı → sağ-altta HEP görünen dikey `#v3dZoomBar` (2B editör zoom kontrolünün 3B ikizi: ＋/dikey slider/−, dark palet, inline SVG, EMOJİ YOK); `wireZoom` overlay kurulunca 1× bağlanır; ＋/− tekerlek adımıyla (×0.83/×1.205) `setDistanceTarget` → tekerlek/pinch/slider çift-yön senkron. **A3 ODAKLAN:** mobilya/kamera seçilince controls hedefi objeye kısa tween'le kayar (**zoom mesafesi SABİT**); `focusFurn`(pos MUTLAK→dünya-cx,-cz)/`focusCam`(target'a); `selectFurn/selectCam`→focus; klavye **F** + panel düğmeleri (`furnfocus`/`camfocus`). **A4 KUŞBAKIŞI KİLİDİ (kalp):** kamera/mobilya grubuna girişte varsayılan **üst açı** (`TOP_PHI≈0.055 rad`, neredeyse tepeden ama derinlik ipucu kalır) yumuşak `tweenToAngle` (mesafe+azimut korunur, yalnız phi) + **döndürme kapalı** (`controls.noRotate`→sol-sürükle PAN), pan+zoom serbest; görünür `#v3dLockBtn` (kilitli=gold/lock, açık=lockopen) tık=aç(serbest, kaldığın açıdan, snap yok)/tık=kuşbakışına tween+kilitle; gruba her girişte KİLİTLİ (kullanıcı kararı), grup dışına çıkınca kilit kalkar `freeSavedView` geri gelir; giriş yolları `setGroup`+`setCamUI`. **KARAR (Fable, kullanıcı onayladı):** mobilya da kuşbakışı-varsayılan. **SERT SINIRLAR korundu:** snapCameraDataURL/snapDataURL/exportCameras/worldToPx px_delta=0 DOKUNULMADI (kilit yalnız interaktif controls), kamera-yerleştirme placeMode(`controls.enabled=false`)+raycaster DEĞİŞMEDİ, yerleştirme UI akışı yeniden tasarlanMADI, MOTOR DOKUNULMADI. **Kapılar:** npm test 37 dosya + snapshot 7/7 + checks-metin 7/7 BİREBİR + build OK. **Tarayıcı (gerçek olaylar):** furniture girişi→kilit(gold)+top-down tween; aç→döndürme serbest(3/4 tilt); re-lock→top-down; kilitliyken sol-sürükle PAN; zoom overlay+slider+düğme her görünümde uygular. **TUZAK (preview):** hidden-tab rAF-PAUSE → `getView()` bayat okur/loop durur (yanlış "donmuş" izlenimi); screenshot foreground'lar → gerçek durum orada; py http.server disk-cache → test için `?cb` cache-bust'lı geçici shell şart (tekdosya `?cb` yetmez, view3d ayrı dosya). |
| **LAYOUT** footer çakışması + histPanel + plansız-atlama | BITTI (2026-07-03, LAYOUT ajanı) | `styles.css` (KPTA) + `mesken/02_PROTOTIP/prototip.template.html` (gitignored, DURUM.md §8) | 3 küçük iş. **(1) Footer çakışması (asıl iş):** kullanıcı raporu "3B butonları + Örnek Sınır footer altında kalıyor" canlı repro edildi (`MESKEN-prototip.html?adim=2`, gerçek DOM ölçümü). Kök neden 3B overlay/footer DEĞİL (0 overlap ölçüldü) — KPTA'nın kendi `#toolbar` (dikey ikon-rayı, `styles.css`) max-height/scroll'suz; standalone KPTA'da sorun yok ama Mesken iframe'i `.workarea` yüksekliğine (pencere−titlebar72−footer64) sıkışınca ray boyu (~700px/17 buton) taşıp `#tSample`/`#t3d` KPTA kabuğunun `body{overflow:hidden}`'ı tarafından tamamen kırpılıyordu (ölçüldü: iframe innerHeight 424 iken toolbar bottom 754). Fix: `#toolbar{max-height:calc(100vh - 20px); overflow-y:auto}` + ince scrollbar — ray artık kayar, buton kaybolmaz; standalone'da görünüşte değişiklik yok. **Yan bulgu/fix:** aynı kökten `#histPanel{left:64px}` (7584fdf) teorik toolbar-sağı hedefliyordu ama toolbar gerçek genişliği (63px, border+padding dahil) yorumdaki "~54px" varsayımını aşıyordu → **9px gerçek çakışma** ölçüldü, `left:64→83px` ile düzeltildi. **Kapsam dışı flag'lendi:** `#floorTabs/#blockTabs/#parkBar/#modeBadge` aynı `left:64px`'i paylaşıyor (teorik aynı risk, bu oturumda tetiklenmedi/değiştirilmedi, ayrı görev). **(2) Plansız atlama şekerlemesi:** `?adim=N` (N≥2) dev-kısayolu iframe `srcdoc` asenkron yüklenmeden `state.step` atıyordu → `openView3D()` planı boş bulup `view3d.js` "Önce bir yerleşim oluşturun" alert'ine çarpıyordu (sessiz no-op). Fix: `_devStep` module-level'e taşındı, `initEngine()`'in mevcut `load` handler'ına `if(_devStep>1){ ensureLayout(); showStep(); }` eklendi — mevcut `ensureLayout()` (YENİDEN YAZILMADI) iframe TAM yüklendikten sonra çağrılır. `?adim` yoksa (normal akış) davranış değişmedi. **(3) KPTA geçmiş-panel çift-kontrolü:** `7584fdf` (left:64) canlıda test edilirken YUKARIDAKİ 9px çakışma bulundu ve aynı fix ile giderildi (tek fix, iki görev karşılığı). **Doğrulama (tarayıcı, gerçek DOM/olaylar):** footer/toolbar öncesi-sonrası ekran görüntüleri; `?adim=2/4/5` fresh-load → alert yok, örnek sınır+yerleşim otomatik, doğrudan istenen adım; `?adim` yok → adım 1'de plansız kalmaya devam (regresyon yok); histPanel 500-600px pencerede 10px net boşluk. **Kapılar:** npm test 37/37 + snapshot-regression 7/7 BİREBİR + test:smoke 49 dosya (mevcut FAIL'ler değişmedi) + build OK. |
| **3B-UX-B1** kamera+görüş şov (view3d.js) | KULLANICI ONAYI BEKLIYOR (2026-07-03, Opus 4.8 uygulayıcı) | tracked `view3d.js` (+build `mesken/MESKEN-prototip.html`) · gitignored `render-server.js`/`prototip.template.html` → `DURUM.md §8 B1-4` | 4 iş kalemi, 3B-UX-A üstüne inşa. **B1-1 YÖN KÜRESİ (viewcube):** zoom barının üstünde kalıcı DOM/SVG küre (`#v3dOrb`); üstünde sürükle→orbit (yeni `controls.orbitBy`/`getSph`, NAV hassasiyeti, sabit 220px ölçek); iğne = mevcut azimut (loop'ta `updateOrb`); hazır görüş chip'leri Üst/İzo + kompas K-G-D-B (SVG text, EMOJİ YOK); `orbPreset` kısa tween (`tweenToAngle`). KİLİTLİYKEN küreden İzo/yön/sürükle → `exitTopLock` ("dikine kaldım" çözümü). **B1-2 KAMERA DOCK:** kamera araçları sağ çekmeceden ÇIKTI → alt-kenar özel panel `#v3dCamDock` (koyu yarı-saydam, `renderCamDock`/`updateCamDock`/`wireCamDock`); çekmece grubu emekli (yönlendirme notu). İçerik: kamera şeridi (numaralı çipler, SARAR — yatay scroll YOK, ölçüldü scrollW==clientW), Yerleştir/Ekle/Yön/Taşı/odakla/Sil, Yükseklik (Alçak/Göz/Üst preset + ince metre slider `setCamHeightM`+`customY`), YENİ Bakış açısı (tilt/pitch slider `setCamTilt`+`camTiltDeg`+`applyCamAim` = pitch-koruyan aim), Objektif 16/24/35/50, YENİ Gün saati seçici, katlanır "Render ayarları" (yöntem/prompt/görsel). **B1-3 AIM:** `aimTargetFrom` yatay-zorlaması (y=camPos.y) KALDIRILDI → hedef tıklanan zemin noktası (aşağı bakış serbest, eski doğru davranış); degenerate guard 3B min 0.5m; tilt slider ince ayar. move/add customY+pitch korur. **B1-4 GÜN SAATİ uçtan uca:** `exportCameras`→`time_of_day`; template `postCamera`/`postRenderIso` payload'a `timeOfDay`; render-server `TIME_HINT`(sunrise/midday/golden/night) → iso+camA+camB prompt'una IŞIK cümlesi (C5-R: LOCK/RESTYLE'a DOKUNULMADI, midday=birebir eski). **SERT SINIRLAR:** snapCameraDataURL(1440×810)/snapDataURL/exportCameras/worldToPx px_delta=0 DOKUNULMADI; raycaster matematiği DEĞİŞMEDİ (B1-3 yalnız hedef-Y); mobilya paneline DOKUNULMADI; styles.css'e DOKUNULMADI; EMOJİ YOK. **Kapılar:** npm test 37/37 + snapshot 7/7 BİREBİR + checks-metin 7/7 BİREBİR + build OK. **Tarayıcı (gerçek olaylar, fresh JS ?cb):** küre İzo/Üst preset tween (screenshot); dock ekle-yön-taşı-sil akışı + place-mode vurgusu; tilt +20°→target yukarı / -40°→target aşağı (measured 1:1); fine height 2.2m preset'i deselect eder; gün saati golden→cam1 override, cam2 midday (global) doğrulandı; yatay scroll YOK. **B1-4 PARALI YAPILMADI (kredi):** dummy-token render-server log `time=golden` + debug prompt sonu "golden hour..." + 401'de durdu (0 kredi); kanıt render kullanıcıya. **TUZAK:** preview hidden-tab rAF-pause → `getView()`/orbit-drag bayat okur (loop durur); screenshot foreground'lar → tween orada görünür (belgeli). |
| **3B-UX-B2** mobilya paleti + dock (view3d.js) | KULLANICI ONAYI BEKLIYOR (2026-07-03, Opus 4.8 uygulayıcı) | tracked `view3d.js` (+build `mesken/MESKEN-prototip.html`) | 4 iş kalemi, B1 tasarım dili + altyapısını (dock/orb/NAV/kilit/tween) YENİDEN KULLANIR; kamera dock/küre/NAV/aim/gün-saati (B1+A) DOKUNULMADI. **B2-1 MOBİLYA PALETİ (yan bayraklar emekli → alt dock):** mobilya araçları sağ çekmeceden ÇIKTI → alt-kenar `#v3dFurnDock` (kamera dock görsel ikizi: `.dk/.col/.sep/.lbl/.seg`). İçerik: KATEGORİ sekmeleri (FURN_PALETTE'ten türer: Oturma/Yemek/Yatak/Mutfak/Banyo/Giriş-Çalışma) + kategori-içi parça ızgarası (`furnThumb` = FURN_DIM w/d oranlı KUŞBAKIŞI şematik SVG küçük-resim + TR ad; ön-yön çizgisi; EMOJİ YOK) + Otomatik (Yeniden döşe/Temizle) + Render on/off + hint. **HAYALET yerleştirme** (`startFurnGhost`/`moveFurnGhost`/`dropFurnGhost`/`cancelFurnGhost`): parçaya tık → kuşbakışı-kilitli görünümde imleci izleyen yarı-saydam mesh (opacity .55; geçerli=normal, geçersiz=KIRMIZI `furnDragValid`/`furnFits`); tık=bırak (gerçek mobilya, seçili), Esc/sağ-tık=vazgeç, bırakınca hayalet ÇIKAR (spam yok; tekrar için palete tekrar tıkla). Placeholder `data-furnact`/`v3dFurnStrip`/`v3dFurnCtl`/eski `Düzenlemeyi aç` toggle EMEKLİ. **B2-2 SÜRÜKLE-vs-PAN:** kural NET — `attachPicker` pointerdown yalnız `furnPickIdx>=0` (mobilya ÜSTÜNDE) ise `beginFurnDrag`; BOŞ zeminde orbit/pan'e düşer (kuşbakışı kilidinde noRotate → sol-sürükle PAN). Mobilya seçili olsa bile boş alan pan'dir (kullanıcı şikayeti). Hit önceliği mobilya>zemin. **B2-3 YÜZEN MİNİ ARAÇ ÇUBUĞU** (`#v3dFurnBar`, `updateFurnBar` loop'ta projekte eder, ekran kenarında clamp): seçili parçanın üstünde Döndür −90/+90 (tekerlek+R KALIR), Çoğalt (Ctrl+D), Odakla (F, A3 tween), Sil (Del) — panel-tabanlı eski akışın yerini alır. **B2-4 OTOMATİK MOD ÇIKIŞI:** furnMode = "mobilya grubu aktif mi" (`syncFurnModeToGroup`); başka gruba/moda/adıma/3B-kapanışa geçişte furnMode+seçim+yarım hayalet otomatik temizlenir (`setGroup`/`setCamUI`/`setPlaceMode`/`close`); kamera placeMode simetrik kapatılır; iki dock aynı anda görünmez. **TUZAKLAR UYULDU:** MUTLAK-metre pozisyon (drag'e +cx/cz eklenmedi), `__w/__d` akışı, `furnDoorBlocked`+`furnFits` SAT, lazy `ensureFMAT`, `window.__kptaFurniture` kalıcılık, io.js `furniture[]` şeması DEĞİŞMEDİ. **SERT SINIRLAR:** snapCameraDataURL/snapDataURL/exportCameras/worldToPx px_delta=0 DOKUNULMADI; export/motor DOKUNULMADI; styles.css DOKUNULMADI (3B stili view3d inline). **Kapılar:** npm test 37/37 + snapshot 7/7 BİREBİR + checks-metin 7/7 BİREBİR + build OK. **Tarayıcı (gerçek olaylar, fresh built prototip ?cb, adım 2+4):** palet aç → 6 kategori + kuşbakışı küçük-resimler; kategori gez (Oturma↔Yatak↔Mutfak); hayalet başlat (cursor=copy, palet vurgusu, hint); GEÇERLİ drop → 0→1 mobilya + seçili + mini-çubuk parçanın üstünde; GEÇERSİZ (plan dışı) drop → RED (sayı sabit, "Geçersiz konum — kırmızı" hint, hayalet aktif kalır); Esc → hayalet iptal; mini-çubuk rotate 0→90° / çoğalt 1→2 / sil 2→1; boş-zemin drag → mobilya DEĞİŞMEDİ (drag-vs-pan ayrımı); grup değişimi → hayalet+seçim+mini-çubuk temizlendi; adım-4 kamera↔mobilya geçişi → tek dock görünür (çift dock yok). **TUZAK:** preview py http.server disk-cache → built prototip `?cb` cache-bust ile taze; hidden-tab rAF-pause loop durdurabilir (screenshot foreground'lar). |
| **3B-UX-B1R** kamera dock revizyonu + canlı PiP (view3d.js) | KULLANICI ONAYI BEKLIYOR (2026-07-03, Opus 4.8 uygulayıcı) | `f67dafb` · tracked `view3d.js` (+build `mesken/MESKEN-prototip.html`) | Kullanıcı B1 dock'unu ekran görüntüsüyle REDDETTİ ("intuitive değil intrusive; panel ekranın yarısı, kamera ÖNİZLEMESİ yok"). 3 iş kalemi, tek dosya (view3d.js). **R1 KOMPAKT DOCK:** eski 3-sütun hep-açık dock → tek yatay ana bar (`.bar`): kamera şeridi (8 çip tek satıra sığar, strip max-width 340px) + Yerleştir/Ekle + seçili-kamera ÖZET çipi (`camSummaryText` "K2 · Göz 1.60m · 24mm · Gece") + Yön/Taşı/Odakla/Sil KOMPAKT ikon düğmeleri (`.ib`). Detaylar (Yükseklik preset+slider / Bakış açısı / Objektif / Gün saati / Render ayarları) VARSAYILAN KATLI — özet çipe tıkla → detay katmanı (`.det`, `camDockDetOpen`); Render ayarları kendi içinde ikinci katlanır (`.adv`, `camDockAdvOpen`). **BULGU/DÜZELTME:** ilk denemede `.detopen`/`.advopen` CSS'i `#v3dCamDock.detopen`'e bağlıydı ama sınıf `.dk` üstünde → detay hiç açılmadı; selektörler `.dk.detopen .det` / `.dk.advopen .adv`'e düzeltildi. **ÖLÇÜM (tarayıcı, offsetHeight):** kapalı **136px** / açık **320px** (brief hedefi 56/160'ın üstü ama rejection nedeni — "ekranın yarısı" — tamamen çözüldü; ekstra yükseklik yerleştirme UI'ını KORUMAK için, kullanıcı onu seviyor). **R2 CANLI PiP (asıl eksik):** kamera SEÇİLİYKEN sol-altta 16:9 pencere ("Kx görüşü") = o kameranın CANLI görüşü. `renderPip()` ana loop'ta ana pass'ten SONRA İKİNCİ scissor pass: ayrı `pipCam` (ana `cam` DOKUNULMAZ), `#v3dPipBody` DOM dikdörtgeni → device-px (Y-flip), `setViewport/setScissor + setScissorTest(true)` → yalnız o dikdörtgene render; gizmo/etiket gizle + çatı kapat (iç mekan, snapCameraDataURL disiplini); pass sonunda viewport/scissor/scissorTest + roof/label/gizmo BİREBİR restore. **KRİTİK FIX:** PiP gövdesi + container ŞEFFAF olmalı (opak div canvas render'ını örter → siyah kalır); yalnız header opak, gövde `background:transparent` = canvas'a delik. Büyüt/küçült (`pipBig` 1.9×) + kapat (`pipClosed`); kamera seçimi kalkınca/kamera-UI kapanınca kaybolur, yeni seçimde `openPipForSelection` geri getirir. Yalnız `shouldShowPip()` (kamera grubu + seçili kamera) true iken render edilir. **TUZAK:** `_snapBusy` bayrağı `snapCameraDataURL`/`snapCameraDepthMap` içinde true → PiP pass ATLANIR (o fonksiyonların setSize 1440×810/restore'uyla çatışma önlenir; senkron JS zaten iç içe girmez ama guard belt-and-suspenders). **R3 TEMİZLİK:** emekli kamera/mobilya çekmece yönlendirme notu (`groupHTML` camera/furniture) kaldırıldı → rail ikonu dock'u DOĞRUDAN açar (`renderDrawer` bu gruplarda çekmeceyi gizler); ölü delege dalları silindi (`data-furntype/furnsel/furndel/furndesel/furnact/camdel` — hiçbir öğe yaymıyor, grep 0) + orphan `setFurnType`/`toggleFurnMode` fonksiyonları kaldırıldı. **SERT SINIRLAR korundu (tarayıcı doğrulandı):** `snapCameraDataURL` 1440×810 (dpr=2'de bile), `exportCameras` render_method=snapshot + room_id + x_px/x_norm/... şeması DEĞİŞMEDİ; worldToPx px_delta=0; kamera yerleştirme/aim/tilt/gün-saati MANTIĞI + küre/zoom/kilit (A/B1) + export/motor/styles.css DOKUNULMADI; EMOJİ YOK. **Mobilya dock (318px) B2 ikizi olarak BIRAKILDI** (palet ızgarası doğası gereği büyük; reshape B2'nin doğrulanmış drag/ghost akışını riske atardı → ERTELENDİ, kullanıcı isterse ayrı tur). **Kapılar:** npm test 37/37 + snapshot 7/7 BİREBİR + checks-metin 7/7 BİREBİR + build OK. **Tarayıcı (gerçek olaylar, fresh built prototip ?cb, adım 4):** dock kapalı 136px/açık 320px ölçüldü; PiP K1 (ahşap/sarı) → K2 seçince (yeşil oda) CANLI değişti; PiP büyüt 232→441px / kapat (display:none) / re-select geri geldi ("K2 görüşü"); snapshot 1440×810; exportCameras render_method=snapshot room_id=D1-living; furniture rail → tek dock (kamera dock+PiP gizli, çekmece yok). **TUZAK:** `.click()` sonrası `getBoundingClientRect` render-preview img reflow'unda BAYAT/yanlış okur (573/1311 gibi) → kararlı ölçüm `offsetHeight` + screenshot-forced frame; py http.server cache → built prototip `?cb`. **NOT (commit):** R1/R2/R3 tek dosyada iç içe hunk → ayrı commit yerine tek B1R commit'i (bölünmüş ara-durum bozuk build riski). PUSH EDİLMEDİ. |
| **3B-UX-C1** footer + odak/dock + kapı kanadı + prompt | KULLANICI ONAYI BEKLIYOR (2026-07-03, Opus 4.8 uygulayıcı) | `fbf8295`(C1-1)+`70ccb83`(C1-2)+`b875bbe`(C1-3)+`cd31a13`(C1-5) · gitignored `render-server.js`(C1-4)→`DURUM.md §8 3B-UX-C1` | 5 iş kalemi (kullanıcı şikayetleri + SS). **C1-1** (tracked `styles.css`+`interaction.js`) KPTA toolbar taşma göstergesi: kök neden ÖLÇÜLDÜ (3 pencere, gerçek DOM) — 81a6906 box-clamp DOĞRU (toolbar hiç #status/canvas altına taşmıyor, offsetParent=canvasWrap = #status ile aynı; sync kopya md5-özdeş, taze), sorun KEŞFEDİLEBİLİRLİK (macOS overlay-scrollbar at-rest gizli + clamp'li kutu alt kenarı "footer" gibi). Fix: her-zaman-görünür ince scrollbar + `.tb-overflow` alt-solma mask (`.tb-atbottom` en altta kapatır) + `syncToolbarOverflow`; tall viewport'ta sınıf/mask yok (standalone değişmedi). Ölçüm: 1280×700/1200×640/1440×960 → scroll sonrası 3B+Örnek sınır erişilebilir, 14px status boşluğu korunur. **C1-2** (tracked `view3d.js`) odak dock arkasında kalmasın: `activeDockPx()`+`focusOnWorld` hedefi ekran-yukarı ekseninde dünya-uzayında aşağı kaydırır (obje görünür-alan ortasına), `updateFurnBar` mini-çubuğu dock üstüne clamp; zoom mesafesi sabit; dock kapalıysa offset=0 (regresyon yok). Ölçüm: odak SY 610→193 (dock top 332 üstü), çubuk 168. **C1-3** (tracked `view3d.js`) kapı KAPALI kanadı: `addDoorLeaf` (panel+kasa+kol, ahşap ton, bina girişi koyu); sahne mesh → iso snapshot+kamera B'ye otomatik (22 kapı, snapDataURL PNG'de görünür doğrulandı); SPAN mantığı DOKUNULMADI; dollhouse'ta duvarla kısalır. **BİLİNEN BOŞLUK:** `iso_render.py` headless kanat parity YOK (kapsam dışı, sandbox fallback). **C1-4** (gitignored `render-server.js`, DURUM kayıtlı) DOLAP≠KAPI: `FURN_NOT_DOOR` → isoPrompt+camSnapshotPrompt(B)+camPromptA generic (3 kullanım); C5-R dersi korundu (LOCK/RESTYLE'a dokunulmadı, kullanıcı-prompt verbatim). Kanıt: dummy-token 4 debug dump (iso/camB/camA-generic'te clause var, LOCK yerinde), tümü 401 = 0 kredi. **C1-5** (tracked `view3d.js`) PiP scissor hizalama: kök neden three.js setViewport/setScissor LOJİK-px alıp pr ile içeride çarpar, renderPip *pr ile ÇİFT çarpıyordu (curViewport CSS iken scissor device) → PiP 2× taşkın; fix *pr KALDIRILDI (CSS piksel), Y-flip korundu, rect her frame yeniden ölçülür. Doğrulama: normal+büyüt+resize → render kutuyu tam doldurur, bodyMatchesScissor=true. **SERT SINIRLAR (hepsinde):** snapCameraDataURL 1440×810/snapDataURL/exportCameras/worldToPx px_delta=0 DOKUNULMADI; küre/kilit/NAV/aim/gün-saati mantığı değişmedi; EMOJİ YOK. **Kapılar:** npm test 37/37 + snapshot 7/7 BİREBİR + checks-metin 7/7 BİREBİR + build OK. Debug hook'ları (view3d `__dbg*`) doğrulama sonrası SİLİNDİ. PUSH EDİLMEDİ. |
| **3B-UX-C2** mobilya paleti glif'leri (view3d.js) | KULLANICI ONAYI BEKLIYOR (2026-07-03, Opus 4.8 uygulayıcı) | `93d58bf` · tracked `view3d.js` (+build `mesken/MESKEN-prototip.html`) | Kullanıcı şikayeti: palet küçük-resimleri "hepsi rastgele dikdörtgen" — tanınmaz. Tek iş kalemi: `furnThumb(type)` (eski: yalnız w/d oranlı düz kutu+ön-yön çizgisi) artık **41 benzersiz katalog tipi için TANINABİLİR kuşbakışı şematik glif** üretiyor. Mimari: `ftBody/ftFront/ftLine/ftRect/ftCircle` küçük SVG-parça yardımcıları + `FURN_GLYPH_MAP` (tip→2-5 iç çizgi fonksiyonu, `_seat`/`_bed` ortak aile helper'ları oturma/yatak grupları için paylaşılıyor) + `furnThumb` gövde+ön-yön çizgisini HER ZAMAN çizip üstüne `FURN_GLYPH_MAP[type]` varsa iç detayı ekliyor. **SÖZLEŞME KORUNDU:** w/d oranı FURN_DIM'den geliyor (parametrik, gliften bağımsız — `bw/bd` hesap satırları dokunulmadı), ön-yön turuncu çizgi (+Z/alt kenar) aynı yerde, box 28×28, tek tema rengi (`#c9a16b` gövde/stroke + `#e0843a` ön-yön vurgusu, dock paletiyle uyumlu), EMOJİ YOK, ince stroke (1.1-1.6px)/yuvarlak köşe (rx), 2-5 çizgi (ikon disiplini). **Glif eşlemesi (aile→tip):** oturma (sofa_2/3, sectional_l, armchair = gövde+sırt çizgisi+kol çizgileri; sectional_l ek L-kanat çizgisi) · yatak (bed_single/double/queen/king = şilte+1-2 yastık+battaniye katlama çizgisi, pillow sayısı tipe göre) · masa/sehpa (coffee_table/side_table/dining_table_4/6/desk/vanity/console/sideboard/bistro benzeri = iç dikdörtgen/daire ± bacak çizgileri) · sandalye (dining_chair/office_chair/bistro_chair = ortak `_seat` sırt çizgisi, kolsuz) · dolap/gövde (wardrobe_2/3/4/bookcase/dresser/nightstand/shoe_cabinet/tv_unit = kapı ayrım çizgi(ler)i veya raf çizgileri, kapı sayısı boyuta göre) · mutfak (counter/island/fridge/oven_hob/dishwasher/sink = kendine özgü ikon: buzdolabı çift-kapı, ocak 2 göz dairesi, evye+musluk) · banyo (toilet=hazne+oval kase, washbasin=daire+musluk, bathtub=iç oval, shower_tray=duş başlığı çizgisi, washer=daire kapak) · saksı (plant=daire+3 yaprak çizgisi) · giriş (coat_rack=merkez+3 kol çizgisi askı motifi, shoe_cabinet=raf çizgileri). **Bilinmeyen tip fallback KORUNDU** (node ile izole test edildi): `FURN_GLYPH_MAP[type]` yoksa `inner=''` → eski davranışla BİREBİR düz kutu+ön-yön çizgisi, hata yok. **DOKUNULMADI:** sahne mesh'leri (`buildFurnMesh`), FURN kataloğu boyut/verisi (`FURN_DIM`/`FURN_PALETTE`/`FURN_TR`), io.js şemaları, kamera dock/PiP/C1 işleri, styles.css. **Kapılar:** npm test 37/37 + snapshot-regression 7/7 BİREBİR + checks-metin 7/7 BİREBİR + build OK (motor davranışına dokunulmadığından beklenen: sıfır fark). **Tarayıcı (gerçek DOM, fresh built prototip `?cb`, adım 4 → mobilya dock → 6 kategori tek tek gezildi):** Oturma(12)/Yemek(4)/Yatak(9)/Mutfak(6)/Banyo(5)/Giriş-Çalışma(6) = 42 palet satırı (41 benzersiz tip, Konsol iki kategoride) — hepsi ayırt edici glif gösterdi, ekran görüntüleriyle doğrulandı. **TUZAK (bu oturumda yaşandı):** ilk toplu-veri-toplama denemesinde `Array.from(...).forEach` ile kategori butonlarına sırayla `.click()` çağrısı YANLIŞ sonuç verdi (her kategori "Oturma" verisini tekrarladı) — kök neden her `.click()` sonrası `renderFurnDock()` DOM'u `innerHTML` ile yeniden yazıyor, önceden alınan NodeList/eleman referansları geçersiz kalıyor; düzeltme: her kategori indeksini AYRI `querySelectorAll(...)[i]` ile TAZE sorgulamak. py http.server cache tuzağı için `?cb` kullanıldı; geçici doğrulama dosyası (`_tmp_furn_glyph_catalog.html`) iş bitince SİLİNDİ. |
| **3B-UX-C3** hayalet ofset + dock etiket + üst hizalama + tavan + dolap/kapı + PiP durum makinesi (view3d.js) | KULLANICI ONAYI BEKLIYOR (2026-07-04, Opus 4.8 uygulayıcı) | `cf29b9c` · tracked `view3d.js` (+build `mesken/MESKEN-prototip.html`) · gitignored `mesken/02_PROTOTIP/server/render-server.js`(C3-5 prompt, `.bak-20260703-C3-5`)→`DURUM.md §8 3B-UX-C3` | Kullanıcı test turundan **6 iş kalemi**, hepsi `view3d.js` (interleaved tek-dosya hunk + monolitik prototip → B1R precedent'iyle TEK commit). **C3-1 HAYALET OFSET (kök neden):** `moveFurnGhost` hayalet mesh'i `__furnitureGroup` çocuğu (grup zaten `-cx,-cz` ofsetli) ama `m.position.set(x-cx,z-cz)` = **ÇİFT ofset** → hayalet `(cx,cz)`=(16,8) SOL-ÜSTE kayıyordu; bırakma yolu (`dropFurnGhost`→`renderFurniture`, MUTLAK pos) doğruydu (o yüzden "tıklanan yere doğru yerleşiyor"). Fix: `m.position.set(x,0,z)` (`renderFurniture`/`moveFurnDrag` ile birebir). **Tarayıcı kanıtı:** geçerli drop'ta ghost render-dünyası == düşen mobilya render-dünyası (`match:true`, 4 köşe + merkez pointermove; algebra `pos-(cx,cz)` ortak). **C3-2 DOCK ETİKET:** `placeBtnLabel()` — placeMode açık=**Bitir**; seçili kamera var=**Düzenle** (mevcut aim akışına girer, `setPlaceMode`→`placeAction='aim'`); yok=**Yerleştir** (yeni kamera akışı değişmedi). `renderCamDock`+`updateCamDock` iki nokta. Görsel doğrulandı ([Düzenle] K1 seçiliyken). **C3-3 ÜST HİZALAMA:** `topAzimuth()` bina EN UZUN kenarını ekran ALT kenarına paralel yapar — plan bbox eksen-hizalı, baskın eksen X/Z → ekran-yukarı=(-sinθ,-cosθ), uzun kenar ⟂ yukarı → `θ=atan2(-dz,dx)` (X-dom→0, Z-dom→-π/2); `enterTopLock`+`orbPreset('top')` `tweenToAngle`'a verilir; `setView('top')` up=-Z(X-dom)/+X(Z-dom). **YALNIZ preset/kilit girişinde** (top-lock zaten noRotate → kullanıcı elle döndüremez, karışma yok). İzo preset DOKUNULMADI. **Kanıt:** X-dom (32×16 sample) görsel uzun-kenar-altta + azim=0/up=-Z; Z-dom (probe 8×16) azim=-π/2/up=+X (measured 1:1). **C3-4 TAVAN:** `scene.__ceiling` grubu (oda-başına düzlem, `y=WALL_H`, mat açık `f3efe6` DoubleSide, VARSAYILAN `visible=false`); YALNIZ `snapCameraDataURL`+`renderPip` pass'lerinde `visible=true`, pass sonu **BİREBİR restore** (mevcut roofOn deseninin ikizi). **SERT KURAL kanıtlandı:** default iso açık-çatı (odalar tepeden görünür); `__dbgCeil(true)` force → iso'da tüm odalar kapandı = tavan render ediyor; camera snapshot'ta iç mekan tavanlı. Ana editör değişmez (checkbox full-height walls açsa bile tavan gizli). **C3-5 DOLAP≠KAPI (mesh):** `addDoorLeaf` kanadı KOYU ceviz (`0x5a3d26`/ext `0x3d2817`) + KOYU kalın kasa (`0x2e2016`, jw 0.06→0.08) + **üst lento çerçeve** + **iki panel kayıt çizgisi** + parlak kol (`0xc8c8cc`) → framed door okunur. Dolaplar `FMAT.cabinet` (`0xcdb089` açık ahşap) + `cabinetMesh` BELİRGİN kapak-ayrım (0.012→0.022) + her kapağa panel çerçeve imi + dikey kulp; wardrobe/bookcase/shoe → cabinet tonu; bookcase raf çizgileri ön-yüze yakın. **Prompt (gitignored `render-server.js`, `.bak`):** `FURN_NOT_DOOR`'a tek cümle (dark framed doors vs light wood cabinets — keep distinct); LOCK/RESTYLE DOKUNULMADI (C5-R dersi). **Kanıt:** interior snapshot'ta koyu kapı vs açık dolap (raf/kapak çizgili) BARİZ. **C3-6 PiP DURUM MAKİNESİ (deterministik):** `shouldShowPip` ⇔ `camUIEnabled` + `activeGroup==='camera'` + seçili kamera + `!pipClosed`. X → `pipClosed=true`; **HERHANGİ seçim eylemi** (çip/sahne/AYNI kamera) `selectCam` başında `pipClosed=false` (+`openPipForSelection`) → geri gelir; **kamera grubuna (yeniden) giriş** `setGroup`'ta `activeGroup==='camera'&&prev!=='camera'` → `pipClosed=false` (kök neden: eski setGroup grup-tekrar-girişinde X'i temizlemiyordu → seçili kamera olsa da PiP susuyordu = "bazen gelmiyor"). `_snapBusy` tüm set/reset zaten try/finally (snapCam+depth, 8-export sonrası strand YOK). **Kanıt (`__dbgPip` predikat, rAF-pause-bağışık):** initial=true→X=false→**aynı-K1-tık=true**→X=false→**K2-tık=true**; kapat→furniture→cam-reenter=true; exportCameras(8)→true. **SERT SINIRLAR (tarayıcı doğrulandı):** `snapCameraDataURL` 1440×810 + `exportCameras` şeması + `worldToPx` px_delta=0 + iso açık-çatı reçetesi + küre/kilit/NAV + kamera aim/tilt/gün-saati + motor + `styles.css` DOKUNULMADI; EMOJİ YOK. **Kapılar:** npm test 37/37 + snapshot 7/7 + checks-metin 7/7 BİREBİR + build OK. Debug hook'ları (`__dbgCeil/TopAzim/Pip/Ghost`) doğrulama sonrası SİLİNDİ. **TUZAK:** py http.server cache → built prototip `?cb`; hidden-tab rAF-pause → `pip.style.display` bayat okur (predikat `shouldShowPip()` ile test edilmeli, DOM display DEĞİL); 0×0 viewport → snapCameraDataURL null (transient). PUSH EDİLMEDİ. |
| **3B-UX-C3-5b** kitaplık mesh düzeltmesi (view3d.js) | KULLANICI ONAYI BEKLIYOR (2026-07-04, Opus 4.8 uygulayıcı) | tracked `view3d.js` (+build `mesken/MESKEN-prototip.html`) | Kullanıcı C3-5'i ekran görüntüsüyle REDDETTİ: "raf iması" DOLU bir levhanın ÖN YÜZÜNDEN DIŞARI çıkıntı yapan çıkmalar olarak eklenmiş (askılık/vestiyer gibi görünüyor) + kitaplığa yanlışlıkla dolap KULPU konmuş (`cabinetMesh(g,W,Dp,D.h,FMAT.cabinet,1)` çağrısı → 1-kapılı kapalı gövde + `Dp/2-0.02`'de dışa taşan raf kutuları). Kök neden: `bookcase` case'i `cabinetMesh`'i (kapı-ayrım çizgisi+kulp veren kapalı-gövde ailesi) yeniden kullanıyordu, kitaplık AÇIK ÖNLÜ olmalıydı. **Fix:** yeni özel `bookcaseMesh(g,w,d,h,mat,shelfCount)` — iki yan panel + üst + alt + ince arka panel (açık önlü karkas, KULP YOK); 3 raf GÖVDENİN İÇİNDE (arka panelden `-pt/2`'de, `shelfD=d-pt-0.02` ile ön yüzden içeride, dışarı taşmaz); her rafta isteğe bağlı 2-4 kitap iması (ince, farklı yükseklik/kalınlık kutular, 4 sönük renk döngüsü `woodDark/cushion/leather/fabric2`) — nano'ya "kitaplık" sinyali güçlensin diye. `buildFurnMesh`'te `case 'bookcase'` tek satıra indi: `bookcaseMesh(g,W,Dp,D.h,FMAT.cabinet,3)`. **Gözden geçirilen diğer tipler (DEĞİŞMEDİ, doğrulandı):** `shoe_cabinet`/`sideboard`/`wardrobe_2/3/4`/`nightstand`/`dresser` hepsi `cabinetMesh` kullanıyor — kapı-ayrım çizgileri ve kulp FLUSH (yüzeyde, `fz=d/2+0.006`, 0.016-0.022 kalınlık), hiçbir eleman ön yüzden dışarı taşmıyor; `tv_unit` grooves da flush. Bu tipler brief'in "kapalı gövdelerde ayrım çizgileri ÖN YÜZEYDE ince koyu şerit (gömme)" kuralına zaten uyuyordu → dokunulmadı. Kapı kanatları (`addDoorLeaf`, C1-3/C3-5) DOKUNULMADI. **Doğrulama:** izole THREE.js harness (geçici, iş bitince silindi) ile view3d.js'ten AYNEN çıkarılan `cabinetMesh`/`bookcaseMesh` fonksiyonları yüklendi; eski (`cabinetMesh+1kapı+dışa-taşan-raf`) vs yeni (açık karkas+içe-raf+kitap) yakın-plan ekran görüntüleri karşılaştırıldı — eski = tam olarak kullanıcının "askılık" şikayeti, yeni = raflar gövde içinde, kulp yok, kitap imalı. shoe_cabinet/wardrobe/sideboard ayrıca render edilip flush/kapalı olduğu görsel doğrulandı. **Kapılar:** npm test 37/37 + snapshot-regression 7/7 BİREBİR + checks-metin 7/7 BİREBİR + build OK (motor davranışına dokunulmadığından beklenen: sıfır fark — yalnız mesh geometrisi, io/export/SAT-footprint aynı). PUSH EDİLMEDİ. |
| **MALZEME** oda-başına zemin/duvar malzemesi (view3d.js + io.js) | KULLANICI ONAYI BEKLIYOR (2026-07-04, Opus 4.8 uygulayıcı) · PUSH EDİLMEDİ · rollback: temiz master üstünde (`git stash`/`checkout`) | tracked `view3d.js`+`io.js`+`tests/malzeme.js`+`run-all.js` (+build `mesken/MESKEN-prototip.html`) · gitignored `render-server.js` (M4 prompt, `.bak-20260704-malzeme`)→`DURUM.md §8 MALZEME` | Kullanıcı isteği: "duvar rengi + zemin seçimi; 3-5 parke, basit dokulu boyalar, ıslak zeminde seramik; kamera render'ının işini kolaylaştırır". **M1 PRESET (texture DOSYASI YOK — canvas'ta PROSEDÜREL, offline):** 14 preset (`MAT_PRESETS`) → zemin parke ×4 (açık meşe/koyu ceviz/gri/balıksırtı, tahta çizgi deseni) + ıslak-zemin seramik ×4 (beyaz/gri/bej/antrasit, fuga ızgarası) + duvar boya ×6 (kırık beyaz/bej/adaçayı/duman mavisi/terracotta/antrasit, düz + hafif noise); `THREE.CanvasTexture` LAZY cache (`matMaterial`, THREE yoksa/headless null → test saf JS); swatch CSS önizleme (`matSwatchCss`). **M2 UI (B1R dock dili, mobilya dock ikizi):** yeni rail grubu "Malzeme" (`swatch` inline SVG ikon, EMOJİ YOK) + alt kompakt `#v3dMatDock` (`renderMatDock`): oda TIKLA (`scenePick` malzeme dalı → floor mesh `userData.roomRef`) → seçili oda tag'ı + Zemin/Duvar swatch ızgaraları (mini önizleme kareleri) + "Varsayılana dön"; ıslak hacim (`isWetRoom` banyo/wc) → zemin listesi SERAMİK ÖNCE. **M3 UYGULAMA + KALICILIK (mobilya sözleşmesinin AYNISI):** `materialOverrides{room_id→{floor,wall}}` runtime + `window.__kptaMaterials` store (`persistMaterials` yazar / `hydrateMaterials` yükler, io.js okur); seçim yalnız O ODANIN malzemesini değiştirir; boot'ta hydrate → save/load + 3B kapat-aç korunur (tarayıcı doğrulandı); aynı swatch'a tekrar tık = kaldır (renk-koda dön). **M4 RENDER (EN KRİTİK — renk-kod=tip sinyali korunur):** SEÇİLMEMİŞ oda BİREBİR eski renk-kodlu (varsayılan sıfır değişim: `colorFor` + paylaşılan `matWall`); SEÇİLİ odada zemin/duvar GERÇEK malzeme (`buildScene` floor mesh `fMat`, `wallEdge(a,b,oWallMat)` per-oda boya — parapet/lento dahil). İso snapshot + kamera B + PiP hepsinde görünür (sahne mesh'i). render-server `isoPrompt`'a tek koşullu cümle (`MAT_CHOSEN`): "Some rooms show real chosen materials (wood/tile floors, painted walls) - keep those materials exactly, do not restyle them" (FURN_NOT_DOOR ile timeHint arasına; **C5-R DERSİ KORUNDU:** SALT açıklayıcı ek, LOCK/RESTYLE ifadelerine DOKUNULMADI; 0-kredi grep=1 kanıtı; `.bak`+DURUM §8). io.js buildFloorplanMap ADDITIVE `materials` alanı (seçilmedi→null; furniture[]/area_m2 şeması BOZULMADI). **SERT SINIRLAR:** motor/checks DEĞİŞMEDİ; pencere/kapı/mobilya sistemlerine DOKUNULMADI (yalnız kullanıldı); export sözleşmeleri (1440×810, worldToPx) aynen; styles.css'e DOKUNULMADI (3B stili view3d inline); EMOJİ YOK. **Kapılar:** npm test **41 dosya** (+`tests/malzeme.js` 14 assert: katalog 14/parke×4/seramik×4/boya×6 · io additive materials · seçilmemiş→null · kalıcılık hydrate+geçersiz-key-elenir · şema-intact) + snapshot **8/8** BİREBİR + checks-metin **7/7** BİREBİR + build OK. **TARAYICI (gerçek olaylar, fresh `?cb` katplan-real:8750):** plan üret → 3B aç → "Malzeme" rail grubu göründü; BANYO odası seç (ıslak hacim tag'ı + seramik önce) → koyu ceviz parke + adaçayı boya UYGULANDI, mesh'te o oda GERÇEK malzeme, diğer odalar renk-kodlu (yeşil/sarı/turuncu); iso snapshot 295KB PNG; 3B kapat-aç → hydrate korundu (getMaterials + io export'ta materials); "Varsayılana dön" → renk-koda döndü; konsol hatasız. PUSH EDİLMEDİ. |
| **PARSEL-A** parsel çizim akışı düzeltmesi + import/export temizliği | KULLANICI ONAYI BEKLIYOR (2026-07-04, Opus 4.8 uygulayıcı) | tek commit (bu satırın commit'i, git log PARSEL-A) · tracked `parsel.js`/`render.js`/`interaction.js`/`app.js`/`io.js`/`planner.js`/`kat-plani-tasarim.html` (+build `mesken/MESKEN-prototip.html`) | Kullanıcı SS+sayıyla teşhis edilen **4 iş kalemi** (davranış-koruyan çevre işi, motor DEĞİŞMEDİ). **P1 BUTON=ÇEKME SINIRI:** "Parsele yapı sınırı çiz" artık kesikli önizlemeyle (`parcelSetback`) BİREBİR aynı çekme zarfını çizer, **TAKS UYGULAMAZ** (eski davranış zarfı sessizce TAKS tavanına büzüyordu → önizleme≠sonuç). `psDrawBuilding` → `psSetbackPoly()`(TAKS'sız zarf) + `psPlaceBuilding()`; etiket "(çekme + TAKS)"→"(çekme sınırı)". TAKS aşımı zaten checks.js kırmızısı (motor kısıtlamaz, uyarır). **Kanıt:** 30×20 parsel, çekme 5/3/3, TAKS 0.2 (cap 120) → çizilen 288 m² = zarf BİREBİR (`ptsEqualsSetback:true`), TAKS uygulanmadı. **P2 TAKS'A SIĞDIR (yeni, pro-only):** `#psFitTaks` `.pro-only` düğmesi — eski TAKS-büzme mantığı ayrı opsiyonel eyleme taşındı (`psFitToTaks`, undo'lu, merkez-ölçekli). **Kanıt:** basic mod display:none / pro inline-block; 288→**120 m²** (cap) tam, "Bina zaten TAKS sınırında"/"İmar TAKS değeri yok" yumuşak durum-çubuğu yolları. **P3 ÇİZİLEN SINIR ANINDA DÜZENLENEBİLİR (kök neden):** bina köşe tutamaçları (`data-bvert`) YALNIZ `renderStructLayer` (yapı modu + `plan` var) çiziyordu → yerleşim+yapı moduna geçene kadar hiçbir köşe düzenlenemez (elle çizim de dâhil). Fix: `render.js` `closed && !plan && mode==='draw'` iken turuncu köşe + kenar-ortası "+" tutamaçları çizer (yapı-modu ailesinin ikizi); `interaction.js` draw-modu mousedown'a `bvert`/`bedge` hit + drag + hover ipucu; drop yalnız `pts` geri-alır (`bounddraw` edit tipi, **generate YOK** — yerleşim öncesi); `psPlaceBuilding` `setMode('draw')` (elle-çizim bitişiyle AYNI durum: mode=draw/closed/plan=null). **Kanıt (gerçek mousedown→move→up):** köşe (3,5)→(5,6.5) taşındı, `bounddraw` history, undo BİREBİR restore; 8 tutamaç (4 köşe+4 kenar). **P4 IMPORT/EXPORT TEMİZLİĞİ:** `aiOutputBtn` UI'dan KALDIRILDI (ControlNet/paint deprecated) — düğme + 7 enable/disable/onclick referansı temizlendi (io.js/app.js/interaction.js/planner.js), ALTTAKİ fonksiyonlar (`exportAIOutput`/`exportAIPaintPNG`/`exportEdgeMaskPNG`/`exportWallBoundaryPNG`/`buildFloorplanMap`) + ai-temiz/gen-floorplan-map testleri AYNEN (motor sözleşmesi+view3d kapı-span parity). Import: "SVG içe aktar"→"İçe aktar", `impFile accept=".svg,image/svg+xml,.json,.dxf"` (DXF algılama io.js'te ZATEN vardı, dosya seçici saklıyordu), fineprint "SVG (plan durumu gömülü ya da eski) · JSON · DXF"; sürükle-bırak değişmedi. **Kanıt:** `kat-plani-ornek-standart.dxf` içe aktarıldı (4 köşe, 480 m², plan kuruldu); AI Output DOM'da yok; fonksiyonlar tip=function. **SERT SINIRLAR korundu:** motor (planner/walls/rooms/checks) DEĞİŞMEDİ; TKGM/imar sağlayıcı (İBB/Ankara/İzmir) + view3d DOKUNULMADI; EMOJİ YOK (`⤿` glifi de kaldırıldı). **Kapılar:** npm test **38/38** + snapshot-regression **8/8 BİREBİR** + checks-metin **7/7 BİREBİR** + build OK (davranış-koruyan → sıfır motor farkı). Tarayıcı: gerçek olaylarla P1-P4 doğrulandı, ekran görüntüleri kullanıcıya. PUSH EDİLMEDİ. |
| **MOBILYA-D** komodin-çifti + ıslak hacim mesh + kitaplık sırtı + çamaşır/bulaşık ayrımı (view3d.js) | KULLANICI ONAYI BEKLIYOR (2026-07-04, Fable 5 uygulayıcı) | `547483d` · tracked `view3d.js` (+build `mesken/MESKEN-prototip.html` — paralel ajanın `a05bb29` hook-build'i çalışma-ağacındaki view3d'yi ZATEN gömmüştü, kaynak commit'i bu satır) | Kullanıcının 4 mobilya kalemi, hepsi `view3d.js` (aynı dosyada iç içe hunk → B1R emsaliyle TEK commit). **M1 KOMODİN ÇİFTİ:** `placeNightstandsFlanking` — komodin(ler) artık genel duvar-taraması DEĞİL, yatağın BAŞ ucu iki yanına `edgeT(hb,bed.pos)` t-koordinatıyla YATAĞA GÖRE kurulur (mutfak gibi ilişkisel); `t=tBed±(bedW/2+0.03+nsW/2)`, duvar hizası `nd.d/2+0.05`, rot=wallRotDeg(hb). İki yan sığarsa ikisi (master cap 2), tek yan sığarsa o taraf, hiçbiri sığmazsa ESKİ genel-duvar davranışına düşer (komodinsiz kalmaz). furnFits/kapı/SAT denetimi AYNEN. **Kanıt (tarayıcı, 32×16 gerçek plan):** EB. YATAK bed_king (14.950,13.750) + komodin1 (15.750,12.595) + komodin2 (15.750,14.905) — bed.z±1.155 TAM SİMETRİK, ikisi de duvar hizasında; küçük yatak odasında (cap 1) tek komodin baş ucu yanında. **M2 ISLAK HACİM:** `bathtubMesh` — küvet artık kapalı kutu+iç-kutu DEĞİL: taban+4 ince porselen duvar (AÇIK tekne), İÇ OYUK yeni `FMAT.tubInner` koyu tonuyla (çukur okunur), üst kenar rim bordürü, kısa kenarda batarya bloğu (gövde+eğik musluk+2 kol). `washbasin` — kabin/tezgah aynen, tezgah üstü çanak artık `basinBowlMesh` basık silindir kase + iç çukur imi + batarya (eski düz dikdörtgen "top" gitti). Dış boyutlar W×Dp×h DEĞİŞMEDİ (furnFits/SAT etkilenmez). **M3 KİTAPLIK SIRTI (kök neden bulundu):** C3-5b arka paneli GERÇEKTEN eklemişti — scene-graph dump'ıyla kanıtlandı (0.84×1.89×0.012 @ z=-0.169, visible:true, konum doğru). Sorun geometri DEĞİL **malzeme kontrastı**: panel gövdeyle AYNI `FMAT.cabinet` açık-ahşap tonundaydı; kısık ambient (0.38) + key-light gölgesinde (özellikle ÜST göz) siyah boşluk gibi okunuyordu = kullanıcının "arkası yok" algısı. Fix: yeni `FMAT.plywood` (0x8a6d47 koyu kontrplak + HAFİF emissive 0x2a1c10×0.5 → ana ışıktan uzak gözlerde bile panel tonu okunur, siyah boşluk imkansız) + raf derinliği arka panelin ön yüzüyle 2mm çakışıyordu (z-fight) → `shelfD=d-pt-0.03`, raf `z=-pt/2+0.005` (temiz ayrım). **Tarayıcı kanıtı:** önden (2.0m, göz hizası) ve yan-üst açıdan kitaplık — üç gözde de sırt paneli NET kahve tonu, eski siyah-boşluk repro'su aynı kamerayla önce/sonra gösterildi. **M4 ÇAMAŞIR ≠ BULAŞIK (mesh+glif):** eski ortak case ayrıştı. washer = ön yüzde YUVARLAK lombar (koyu çerçeve halkası + cam daire kapak) + üst kontrol bandı + 3 düğme; dishwasher = DÜZ koyu ön panel (davul/kapak imi YOK) + üstte YATAY metal kol (tezgah-altı komşusu görünümü). Palet glifi: dishwasher artık dikdörtgen çerçeve + yatay kol çizgisi (washer'ın daire kapağından ayırt edilir; washer glifi değişmedi). **SÖZLEŞMELER korundu:** MUTLAK-metre pozisyon, `__w/__d` parametrik akış, furnFits/furnDoorBlocked/SAT dış boyutları DEĞİŞMEDİ (yalnız iç mesh detayı), io.js `furniture[]` şeması aynen, `ensureFMAT` lazy (yeni tubInner/plywood da içinde), `__kptaFurniture` kalıcılık, motor/styles.css DOKUNULMADI, EMOJİ YOK. Geçici `__dbg*` hook'ları doğrulama sonrası SİLİNDİ. **Kapılar:** npm test **39/39** (paralel ajanın kucuk-kat.js'i dahil) + snapshot-regression **8/8 BİREBİR** + checks-metin **7/7 BİREBİR** + build OK (motor davranışına dokunulmadığından beklenen: sıfır fark). **NOT (doğrulama kapsamı, dürüst):** M1 simetri + M3 önce/sonra tarayıcıda gerçek WebGL ile doğrulandı; küvet/lavabo ve çamaşır-bulaşık yakın-plan SS turu oturum watchdog kesintisiyle kısaltıldı → bu ikisi kod + scene-graph dump seviyesinde doğrulandı, göz kararı SON KAPI KULLANICIDA (C5-R dersi). PUSH EDİLMEDİ. **TUZAK (bu oturum):** `restoreView` çağrısı grup/mod değişikliğiyle AYNI tick'te yapılırsa fitView/damping ezer — önce UI durulsun, sonra tek başına çağır; py http.server cache → `?cb`. |
| **MESH-K** kitaplık sırtı 3.tur + kamera clamp + küvet/klozet + AI-göz taraması + duvar-zemin (view3d.js) | KULLANICI ONAYI BEKLIYOR (2026-07-04, Fable 5 uygulayıcı) | tracked `view3d.js` (+hook-build `mesken/MESKEN-prototip.html`) · PUSH EDİLMEDİ | Kullanıcının SS'li 6 kalemi, hepsi `view3d.js` (aynı dosya iç içe hunk → MOBILYA-D emsaliyle TEK commit). **Q1 KİTAPLIK SIRTI (kök neden 3. turda KESİN bulundu — brief hipotez (a) doğru + 2 ek katman):** İzole harness'te duvar önünde repro edildi. (1) ASIL: duvara snap edilen mobilyanın inset'i `d/2+0.05` = oda-poligon kenarından 5 cm; ama poligon kenarı duvar MERKEZ hattı, duvar kutusu içeri `WALL_T/2=0.10` taşar → mobilya arkası 5 cm DUVARIN İÇİNDE; ince sırt paneli duvar gövdesine gömülü → rafların arasından açık duvar tonu görünüyor ("sırt yok"). FIX `WALL_CLR=WALL_T/2+0.02` yeni varsayılan gap; 4 yerleştirme noktası (furnWallSnap drag-snap, placeOnEdge default wallGap, mutfak tezgah dizisi, komodin flanking) taşındı. (2) Sırt paneli arka yüzü z=-d/2'de yan/üst/alt panellerin arka yüzüyle ÇAKIŞIKTI → z-fight; panel gövde İÇİNE alındı (`backFrontZ=-d/2+pt`, backT 0.012→0.02, raflar panel ön yüzünden başlar). (3) M3 plywood tonu güçlü key-light altında açık ahşaba yıkanıyordu → koyulaştırıldı (0x8a6d47→0x6f5636, emissive 0.5→0.35) + moiré şeritlerinin kökü SHADOW ACNE çıktı (shadow=0 A/B ile kanıtlandı) → `key.shadow.normalBias=0.05` (bias -0.0004 korundu). KABUL SS'i: ön-sol hafif yukarı açıdan duvar önünde kitaplık — sırt NET koyu kontrplak, duvar sızmıyor, moiré yok. **Q2 KAMERA CLAMP:** `CAM_Y_MAX=WALL_H-0.15` (2.55) + `CAM_Y_MIN=0.3`; `setCamHeightM` clamp (eski üst sınır 3.2 = tavan ÜSTÜydü), dock slider max dinamik `CAM_Y_MAX.toFixed(2)`, `setCameras` yüklenen kamera pos.y clamp. Preset'ler (1.1/1.6/2.2) zaten altında. exportCameras şeması DEĞİŞMEDİ. Kod-seviyesi doğrulama (basit clamp); tarayıcı slider turu yapılMAdı (watchdog kapanışı). **Q3 KÜVET GERÇEK BOŞ (kök neden):** eski "rim" DOLU slab'dı (w+0.02 tam ayak izi) = fiilen KAPAK → içi boş görünmüyordu. Yeni: dış gövde + iç tekne bir kademe içeride/alçak tabanlı (üstü AÇIK) + rim 4-şerit FRAME + batarya. SS: üstten VE 3/4'ten oyuk NET. **Q4 KLOZET:** eski 3-kutu=koltuk silüeti → yeni `toiletMesh`: rezervuar kutusu (sifon düğmeli) + OVAL basık-silindir çanak + kapalı kapak plakası + zemine daralan pedestal; beyaz seramik. SS: net klozet. **Q5 AI-GÖZ TARAMASI:** 45 tipin tamamı izole tek-renderer kontakt-sayfasında render edildi (WebGL context limiti tuzağı: 45 ayrı renderer ilk ~30 hücreyi patlatır → tek offscreen renderer + drawImage). RİSKLİ bulunup DÜZELTİLEN 5 aile: **sofa_2/3+sectional (sofaMesh)** yüksek arkalık+kalın minder+şişkin sırt minderi (eski: bench/sedir gibi), **armchair** aynı dil+ayaklar, **sink** tezgah FRAME+gömme çelik tekne+gooseneck (eski: dolap sanılırdı; TUZAK: dolu tezgah slab'ı tekneyi gizler → frame şart), **island** kapak çizgileri+kulplar+taş tezgah overhang, **shower_tray** ön dikey+üst METAL çerçeve+duş başlığı+gider (cam saydam → kutu belirsizdi). SS: yeni sofa/armchair/sink/shower yakın-plan alındı; **island yeni hali SS'siz** (kod portlandı, kontakt yeniden-üretimi watchdog'la kesildi — göz kararı kullanıcıda). KALAN ŞÜPHELİLER (sonraki tur): pouf (düz küp), nightstand (jenerik kutu), counter (ada ile aynı görsel dilde değil), vanity (ayna imi zayıf), coat_rack (soyut). **Q6 DUVAR-ZEMİN SIZINTI:** duvar tabanları zemine 2 cm gömüldü (`seg` y-0.02 + `applyRoof` aynı ofset — çatı toggle'ı y'yi resetliyordu, ikisi birden şart) + normalBias gölge-precision katkısını da keser. İç mekan PiP/iso canlı SS'i ALINMADI (watchdog) — kod-seviyesi, göz kararı kullanıcıda. **SÖZLEŞMELER:** dış boyutlar/furnFits/SAT/io şemaları/snapCameraDataURL 1440×810/worldToPx DOKUNULMADI (inset yalnız KONUM, boyut değil; oda içine doğru = güvenli yön); motor/styles.css el değmedi; EMOJİ yok; texture yok; çamaşır/bulaşık dokunulmadı. **Kapılar:** npm test 39 exit=0 + snapshot-regression 8/8 BİREBİR + checks-metin 7/7 BİREBİR + build OK. İzole harness + geçici launch girdileri SİLİNDİ. **TUZAK (bu oturum):** (1) three.js'te `Object.assign(light,{position:vec})` sessizce patlar → `position.set`; (2) kontakt-sayfada çok WebGL context = ilk hücreler boş; (3) shadow-acne moiré'si z-fight sanılır — ayrım için `shadow=0` A/B'si. |
| **D** | BEKLIYOR | — | ML ranker — ayrı oturum. Tercih verisi VERI-MUSLUGU ile birikmeye başladı. |

**A2 için hazır harita (2026-07-02 grep'lendi; satırlar A1+A3 sonrası, kayabilir → fonksiyon adıyla grep):**
- `core.js`'te `REG` ZATEN VAR (mevzuat sabitleri, satır 6). Layout/onarım sabitleri ayrı alt-bölüm olarak eklenmeli (ör. `REG.iter`, `REG.cap`, `REG.layout`) — mevzuatla karışmasın.
- Guard limitleri: `planner.js repairUnits` `iter<30`; `rectifyCorridor` `pass<24`; `rooms.js slimUnitAntre` `guard++>240`. (rectifyCorridorEnds ve rectifyUnitBalance de `pass<24` kullanır — A2'de bunları da REG.iter'e bağla.)
- Boyut katsayıları (İKİ paralel layout bloğunda tekrarlıyor — `planner.js layoutUnit` ~929 ve ~1093/1113): mutfak `area*0.085`, banyo `area*0.04`, bedCap `area*0.12`, ebCap `area*0.15`. Taban değerleri de sihirli: `Math.max(13,…)` mutfak, `Math.max(6.5,…)` banyo, `Math.max(20,…)` yatak, `Math.max(26,…)` eb.
- Alan eşikleri: `area<=45` / `area>45` (küçük daire), `area>120` (boyut sınıfı, ms 2.5/3.0), `area<=140`+`area>140` (shallowU / köşe-banyo boyutu 5v4), `area>=(hasEns?130:110)` (ensuite eşiği), `bA<0.45*totalIn` (gövde algısı @151).
- Derinlik: `depthM<=9` (@1097 shallowU), cephe payı `2.5`/`2.0`/`1.5` (@~1167 minD). `depthM>=10.5` grep'le doğrula.
- **UYARI:** aynı sayı (120, 140, 45) farklı yerlerde farklı anlam taşıyabilir — kör replace YAPMA; her occurrence'ı semantiğiyle eşle. Her adımdan sonra `node tests/snapshot-regression.js` BİREBİR olmalı.

**A3'ün üstünde bıraktığı KALAN sessiz vazgeçmeler (bu oturumda eklenMEDİ, aday):**
`rectifyCorridorEnds` (`planner.js` ~2027 `pass<24`), `rectifyUnitBalance` (aynı bölge `pass<24`, CV_TARGET break), `carveMissing` host bulamayıp banyoyu atlaması, leftover hücre "büyük odaya" dökümü >~8. Brief A3 bunları da önerdi; ilk turda 3 ana guard yapıldı.

**Her oturum-sonu:** `npm test` (31/31 yeşil olmalı) + `node tests/snapshot-regression.js` (davranış-koruyan işlerde birebir) + `npm run build`.

---

## 0) ÖNCE OKU (zorunlu sıra)

1. `CLAUDE.md` — **"Değişiklik nereye yazılır"** kuralı ve build akışı. Özet: motor mantığı HEP kök `.js` kaynağına; `npm run build` tekdosya + mesken prototip'i otomatik üretir; `mesken/MESKEN-prototip.html`'e ELLE DOKUNMA.
2. `Claude_instructions/DEVIR-NOTU.md` — motor mimarisi ve tarihçe.
3. Ana repo `mesken/DURUM.md` (gitignored, ana repo kökünde: `katplanitasarim/mesken/DURUM.md`) — render reçetesi tek doğru kaynak.
4. Yol haritası planı (yukarıdaki plans dosyası) — bu brief o planın **önünü hazırlar**, onunla çakışmaz. L1-A (kalınlık/brüt-net/DXF-export) işleri bu brief'in kapsamı DIŞINDADIR.

---

## 1) MİSYON VE KAPSAM

**Tez:** Keşif fazı doğru cevapları buldu (mesh-first render, vektör-kernel yönü, region-uzayı ML sinyali). Şu anki risk, bu cevapların **korumasız zeminde** durması: motorun 9-geçişli onarım zinciri elle-ayarlı sabitlerle ve sessiz vazgeçme guard'larıyla dolu, hiçbir regresyon taban çizgisi yok; UI her drag frame'inde tüm planı yeniden üretiyor; render hattında model sürümü sabitlenmemiş; ML yanlış temsilde ısrar etmiyor ama doğru pivota da geçmedi.

**Dört paket** (öncelik sırasıyla; her paket kendi oturumu/commit dizisi olabilir):

| Paket | Ad | Neden önce/sonra |
|---|---|---|
| **A** | Güvenlik ağı (motor + workflow) | HER ŞEYDEN ÖNCE. A1 taban çizgisi olmadan B/C/L1-A'daki geometri değişimleri körlemesine olur. |
| **B** | UI akıcılık | En büyük hissedilir iyileşme; A1 sonrası güvenli. |
| **C** | Render sertleştirme (mesken) | Saatlik işler + bir orta boy özellik (C5). |
| **D** | ML ranker | AYRI OTURUM önerilir; A-C'den bağımsız, paralel yürüyebilir. |

**Kapsam DIŞI (yapma):** TypeScript/React/framework geçişi; ızgara boyutu değişikliği (M=0.5 kalır); L1-A özelliklerinin kendisi (duvar kalınlığı, DXF yazıcı, brüt/net); "Mevzuata Uydur" tam özelliği (B7'de sadece altyapı adımı var); kredi muhasebesi (ayrı oturum kararı var); mevcut sabit DEĞERLERİNİ değiştirmek (A2 sadece taşıma).

---

## 2) ÇALIŞMA KURALLARI (uygulayıcı için)

- **Küçük, doğrulanabilir adımlar.** Her alt-paket sonunda `npm test` yeşil + `npm run build` başarılı. Paket başına ayrı commit(ler); mesaj Türkçe, mevcut commit üslubunda.
- **Davranış değiştirmeyen refactor'larda kanıt zorunlu:** A2 ve A4 için A1'in snapshot-regresyon testi **birebir aynı** çıktı vermeli. Fark çıkarsa dur, nedenini anla, kullanıcıya raporla.
- **EMOJİ YOK** — UI'da, kaynak kodda, bu brief'in ürettiği dosyalarda, hiçbir yerde. İkon gerekiyorsa inline SVG (`icons.js` deseni; TUZAK: `styles.css` global `svg{width:100%}` → boyutu inline style'da ver).
- **Ürün kararı gereken yerde kullanıcıya sor** (aşağıda işaretli: C7). Gerisinde sormadan ilerle.
- Test altyapısı: testler headless Node, `tests/support/app-js.js` script etiketlerini sırayla okur; `npm test` = `tests/run-all.js`. Yeni test eklerken mevcut kendi-kendine-yeten desenini kopyala (`node tests/<dosya>.js` tek başına çalışmalı).
- Tarayıcı doğrulaması gerekirse: preview'da çizim canvas'ı **global `svg`** değişkenidir; `document.querySelector('svg')` toolbar ikonunu döndürür (yanlış handler). Tekdosya önizlemede cache: `tekdosya?cb=<rastgele>`.
- Satır numaraları 2026-07-02 itibarıyladır; kaymış olabilir — **fonksiyon adıyla grep'le**, satıra körlemesine güvenme.

---

## 3) PAKET A — GÜVENLİK AĞI (önce bu, sırayla)

### A1. Snapshot-regresyon testi — `tests/snapshot-regression.js` (EN KRİTİK)

**Amaç:** Motor çıktısının sayısal parmak izini sabitle; sonraki her değişiklik (A2, A4, B, L1-A) bu taban çizgisine karşı doğrulanır.

**Yapılacak:**
1. Sabit girdi seti tanımla (mevcut test senaryolarını yeniden kullan): 32×16 standart, 21×18 4×3+1, L-şekil, 48×27 derin blok, villa 5+1, tek-daire/kat, komb (40×12 sığ). `tests/v22-test.js` ve `tests/room-edit.js`'teki girdi kurulumlarını örnek al.
2. Her girdi için `generate()` sonrası parmak izi çıkar: bölge sayısı; her bölge için `{name, type, unit, area, bbox(w,h), minSide, cellCount}`; daire başına toplam alan; kapı sayısı; `runChecks()` bad/info sayıları.
3. Taban çizgisini `tests/fixtures/snapshot-baseline.json`'a yaz (commit'lenir). Üretim modu: `node tests/snapshot-regression.js --write`.
4. Karşılaştırma: motor deterministik olmalı → **önce birebir eşitlik** dene. Fark çıkarsa kaynağını araştır (gerçek nondeterminizm mi?); ancak belgelenmiş nondeterminizm varsa ±%2 toleransa düş ve nedenini test dosyasına yorum olarak yaz.
5. `tests/run-all.js`'e strict test olarak ekle.

**Kabul:** `npm test` yeşil; baseline JSON commit'li; `--write` olmadan koşunca mevcut motorla sıfır fark.
**Efor:** ~yarım gün.

### A2. Sihirli sayılar → `REG` sabitleri (`core.js`)

**Amaç:** `layoutUnit` ve onarım zincirindeki elle-ayarlı eşikleri tek tabloya topla. **Değer değiştirmek YASAK — sadece taşıma.**

**Taşınacaklar (grep'le doğrula, liste eksiksiz olmayabilir):**
- Alan eşikleri: 45 / 110 / 120 / 130 / 140 m² (kiler, ensuite, boyut sınıfları — `planner.js layoutUnit` ve `checks.js`).
- Derinlik eşikleri: `depthM<=9`, `depthM>=10.5`, cepheye `<2.5` / `<2.8` m payı (komb kararları).
- Boyutlandırma katsayıları: `area*0.085` (mutfak), `*0.04` (banyo), `*0.12` (bedCap), `*0.15` (ebCap).
- Guard limitleri: `repairUnits` 30, `rectifyCorridor` 24, `slimUnitAntre` (rooms.js) 240 — `REG.iter = {repairUnits:30, rectifyCorridor:24, slimAntre:240}` gibi.
- Her sabite bir satır Türkçe yorum: neyi kontrol ediyor, hangi vakadan/dersden geldi (DEVIR-NOTU md.15/18 bağlantısı biliniyorsa).

**Kabul:** A1 snapshot testi **birebir aynı**; `npm test` yeşil.
**Efor:** ~1-2 saat.

### A3. Sessiz vazgeçme uyarıları

**Amaç:** Onarım zinciri pes ettiğinde iz bırakması.

**Yapılacak:** Guard limiti dolan her döngüye `console.warn('[KPTA] <fonksiyon> iterasyon limiti: <oda/daire>')` ekle: `repairUnits` (planner.js ~1541), `rectifyCorridor` (~1704), `slimUnitAntre` (rooms.js ~44). Ayrıca: `carveMissing` host bulamayıp banyoyu atladığında; leftover hücreler "büyük odaya" dökülürken hücre sayısı > ~8 ise. Sadece `console.warn` — UI'a taşıma, testleri kirletmesin (gerekirse `typeof process!=='undefined'` kontrolüyle test ortamında sustur ya da testlerde beklenen uyarıları belgele).

**Kabul:** `npm test` yeşil (uyarılar test başarısını etkilemez); normal üretimde uyarı SAYISI raporlanıp kullanıcıya bildirilir (kaç senaryoda limit doluyor — bu başlı başına bulgu).
**Efor:** ~30 dk.

### A4. Alan hesabı tek kaynak

**Amaç:** `cells.length*M*M` ~40 çağrı noktasında inline; `walls.js:7` (canlı) ile `io.js:501` (export) kopyalı — yol haritası L1-A2'nin ön adımı.

**Yapılacak:** `calcRegionMetrics` zaten `g.area` üretiyor. (1) Bölge metriği MEVCUT olan çağrı noktalarında inline hesabı `g.area` ile değiştir. (2) Keyfi hücre dizisi alanı hesaplayan yerler için `core.js`'e `areaOfCells(cells)` yardımcısı ekle ve oralarda onu kullan. (3) `io.js:501` export hesabını aynı yardımcıya bağla. DİKKAT: bazı noktalar `g.area`'nın BAYAT olabileceği anlarda hesaplıyor (onarım ortası, metrikler yenilenmeden) — böyle yerlerde `areaOfCells` kullan, `g.area` kullanma. Şüphede kalırsan davranışı koru.

**Kabul:** A1 snapshot birebir; `npm test` yeşil.
**Efor:** ~2-3 saat (dikkat işi).

### A5. Performans enstrümantasyonu (opsiyonel bayraklı)

**Yapılacak:** `KPTA_PROFILE` global bayrağı (varsayılan kapalı); `generate()` içinde ana fazları (`inside` doldurma, koridor, çekirdek, layoutUnit toplamı, her onarım geçişi, runChecks, render) `performance.now()` ile ölçüp `console.log('[PERF] ...')`. Ek: `tests/perf-smoke.js` (strict DEĞİL, `npm run test:diagnostics` ailesine) — 48×27 derin blok üretimini 3 kez koşup süreyi basar, eşik koymaz.

**Kabul:** Bayrak kapalıyken sıfır davranış farkı; `npm test` yeşil.
**Efor:** ~1-2 saat.

### A6. CI + (hafif) lint

**Yapılacak:**
1. `.github/workflows/test.yml`: push + PR tetikli; ubuntu-latest, Node 20, `npm install`, `npm test`, `npm run build`. Secret gerekmez (testler headless, build lokal). Repo PUBLIC — workflow'a sır/path sızdırma (yedek scripti deseni: `tools/yedek-al.sh` bilinçli commit'lenmemişti).
2. Lint: tam ESLint kurulumu yerine önce ucuz kazanım — `tests/`'e ya da workflow'a her kök `.js` için `node --check` adımı (DEVIR-NOTU'daki "açık kalan if bloğu tüm script'i kırdı" vakasının otomasyonu). İstersen ikinci adım olarak ESLint flat config + `no-undef` (globals listesi kürasyonu gerekir — app bilinçli global-ağır; ~40 global'i `eslint.config.js`'te `globals` olarak tanımla) + `no-unused-vars: warn`. ESLint kısmı zaman kalmazsa atlanabilir; CI + node --check yeterli ilk adım.

**Kabul:** GitHub Actions ilk koşu yeşil (push sonrası kontrol et); `node --check` tüm kaynaklarda temiz.
**Efor:** ~1-2 saat (+ESLint ~2-3 saat, opsiyonel).

### A7. (STRETCH — zaman kalırsa, yoksa ayrı oturum) `checks.js` kural-tablosu refactor'u

Mevzuat kurallarını veri olarak ayır: `RULES = { SALON_MIN: {check(plan), msg(ctx), severity} ... }`; `runChecks` tabloyu koşar. Amaç üçlü tüketici: (1) checks paneli, (2) ileride DXF katman/rapor export'u, (3) Paket D'de ML filtre/ödül. Davranış birebir korunmalı (panel çıktısı aynı metinler). Büyük iş (~1 gün) — başlarsan yarım bırakma.

---

## 4) PAKET B — UI AKICILIK

> Dokunulan dosyalar: `interaction.js`, `render.js`, `app.js`, `styles.css`, `kat-plani-tasarim.html`, gerekirse `mobile.js`. Motor mantığına dokunulmaz. Her adımdan sonra `npm test` (özellikle `touch.js`, `wall-drag.js`, `cut-preserve.js`) + `npm run build`.

### B1. Cut-drag hayalet önizleme (EN BÜYÜK KAZANIM)

**Mevcut durum (doğrulandı):** `interaction.js:118-121` — ayırıcı (cut) sürüklerken HER mousemove'da `generate(true)` koşuyor (flood-fill + yerleşim + onarım zinciri + checks + tablo). 60 event/sn × 100-300 ms = donma; mobilde kullanılamaz.

**Yapılacak:**
1. Drag SIRASINDA `generate` çağırma. Bunun yerine: `dragging.arr[dragging.idx]` güncellenir (mevcut satır kalır), ama sadece **hayalet çizim**: kesikli ayırıcı çizgisi yeni konumda + imleç yanında canlı ölçü etiketi (ayırıcının iki yanında kalan bant genişlikleri, "6,5 m | 4,0 m" biçiminde; `fmt` yardımcısını kullan).
2. Bırakınca (`finishDrag`, interaction.js ~371-374): MEVCUT commit yolu **birebir korunur** — `generate(true)` + `restoreEditedFootprints(dragging.preUnits)` + undo kaydı. Bu yol `cut-preserve.js` testinin güvencesinde; bozma.
3. Hayalet render'ı ayrı, hafif bir katmanda çiz (render()'ın tam yeniden çizimini tetiklemeden SVG'ye geçici `<g id="cutGhost">` ekle/güncelle; mouseup'ta kaldır).
4. Aynı deseni mavi duvar-node sürüklemesi zaten kısmen izliyor (drop'ta hesap); oraya da canlı ölçü etiketi ekle (oda W×H, `moveWallStep` sonucu beklemeden mevcut bölge bbox'undan).
5. Mobil: sentetik MouseEvent katmanı aynen çalışmalı (`tests/touch.js` yeşil kalmalı).

**Ürün notu:** Canlı yeniden-yerleşim hissi kaybolur (artık drop'ta reflow). Bu bilinçli takas — akıcılık > canlı reflow. Kullanıcı isterse ileride RAF-throttle'lı (~5/sn) canlı mod eklenebilir; İLK sürümde ekleme.

**Kabul:** Cut sürükleme 60fps akıcı (Perf: drag sırasında `generate` çağrısı SIFIR); drop sonrası davranış ve undo birebir eski; `npm test` yeşil.
**Efor:** ~yarım gün.

### B2. Mod rozeti (canvas üstü kalıcı gösterge)

`setMode` (interaction.js ~691) her çağrıda küçük sabit bir rozeti güncellesin: sol-üst, toolbar'ın hizasında, 11-12px, mevcut panel stilinde (`#histPanel` görselini örnek al). İçerik: aktif mod Türkçe adı + tek satır ipucu (ör. "Avlu — sınır içinde sürükleyerek boşluk oy"). Pan/draw gibi varsayılan modda sade kalsın. Emoji yok; gerekirse `icons.js`'ten mod ikonu.

**Kabul:** Her mod geçişinde rozet günceller; mobilde taşmaz (≤700px medya sorgusunda konum kontrol et).
**Efor:** ~1-2 saat.

### B3. Klavye kısayolları

Modlar için tek-tuş kısayollar (modifier'sız): öneri D=çiz, P=parsel, B=balkon, A=avlu, K=kapı, Y=yapı, O=oda çiz, T=park, S=site (çakışmaları kontrol et; mevcut: Space=geçici pan, Esc=iptal, Ctrl+Z/Y — bunlara dokunma, `spacePan`/`syncPanCursor` bozulmasın). Koşullar: `input/textarea/select/contentEditable` odaktayken yut; toolbar tooltip'lerine "(D)" biçiminde ekle (`data-tip` metinleri).

**Kabul:** Kısayollar çalışır, form alanlarında tetiklenmez, tooltip'ler günceller.
**Efor:** ~1-2 saat.

### B4. Tutamaç affordance'ları (turuncu/mavi ayrımı görünür olsun)

**Bağlam:** Turuncu daire-ayırıcı = CUT (`hitCutHandle` → tüm daireleri yeniden dizer), mavi dış-duvar = `moveWallStep` (elle düzeni korur). Bu ayrım görünmez ve geçmişte kullanıcıyı yaktı (cut-handle-reset vakası).

**Yapılacak:** (1) Hover'da tutamaç büyür/parlar (CSS class, render'da hit-test sonucu `hover-handle` işaretle). (2) Hover'da durum çubuğunda tek satır: turuncu için "Daire sınırı — bırakınca iki daire yeniden dizilir", mavi için "Duvar — odalar korunarak kayar". (3) `struct` modunda çekirdek tutamaçları ile bina köşe tutamaçlarını görsel ayır (dolgu rengi/biçim — kare vs daire zaten kısmen var; netleştir).

**Kabul:** Hover geri bildirimi üç tutamaç ailesinde de çalışır; ekran görüntüsüyle doğrula.
**Efor:** ~2-3 saat.

### B5. Stepper debounce

`app.js` stepper (+/− basılı tutma, ~802-811): her tetikte `safeGen`. 200ms trailing debounce ekle (son değişiklikten 200ms sonra tek `safeGen`). Diğer spec input'ları da aynı yardımcıyı kullansın.

**Kabul:** Basılı tutarken UI donmaz; bırakınca tek üretim.
**Efor:** ~30-45 dk.

### B6. Parsel kapanış geri bildirimi

Parsel kapanınca (interaction.js ~284): kontur kesikliden düze dönsün (render.js parsel çizimi, `stroke-dasharray` koşullu) + durum çubuğunda kalıcı ipucu "Parsel kapalı — yol cephesi seçmek için kenara sağ tıkla". Toast altyapısı yoksa kurma; durum çubuğu yeter.

**Kabul:** Kapanış anı görsel olarak fark edilir.
**Efor:** ~1 saat.

### B7. Denetim panelinden eyleme köprü (altyapı adımı)

`checks.js` bad satırlarına opsiyonel `action` alanı: şimdilik SADECE "odağa git + doğru aracı öner" (ör. "Salon 10,5 < 12 m²" satırında düğme → `focusRegion(salon)` + durum çubuğunda "Komşu duvarı sürükleyerek büyütün"). Otomatik düzeltme ("Mevzuata Uydur") BU PAKETTE YOK — o ayrı oturum (altyapı: `runChecks` ihlal listesi döndürüyor, aktüatör `moveWallStep`, şablon `slimAntres` deseni; DEVIR-NOTU bilinen sınır #5).

**Kabul:** En az 3 ihlal tipinde (salon/yatak/mutfak piyes ölçüsü) düğme çalışır.
**Efor:** ~2-3 saat.

---

## 5) PAKET C — RENDER SERTLEŞTİRME (mesken)

> DİKKAT: `render-server.js` ana repoda gitignored alandadır: `katplanitasarim/mesken/02_PROTOTIP/server/render-server.js`. `view3d.js`/`io.js` ise KPTA kaynağı (tracked) — değişiklik sonrası `npm run build` şart (prototip motoru gömer). Mesken-özel şeyler `mesken/build-prototip.js`/template katmanına (CLAUDE.md istisna kuralı).

### C1. Model sürümü sabitleme + seed

`render-server.js:46`: `const MODEL='google/nano-banana-pro'` — sürümsüz, Replicate sessizce model değiştirebilir. (1) Replicate API'den mevcut sürüm hash'ini al, `MODEL@<version>` biçiminde sabitle (ya da `POST /v1/predictions` + `version` alanı yoluna geç). (2) Endpoint `seed` kabul ediyorsa reçetelere `seed:42` ekle; etmiyorsa denemeyi ve sonucu `DURUM.md`'ye not düş. (3) `/health` yanıtına sabitlenen sürümü ekle.
**Kabul:** Aynı girdiyle iki koşu aynı model sürümünü kullanır (log'da görünür); smoke render başarılı.
**Efor:** ~1 saat (+token/ağ kurulumu: DURUM.md §5 proxy kuralları).

### C2. Snapshot çözünürlük kilidi

`view3d.js snapCameraDataURL` (~580): `renderer.domElement` viewport boyutunda render alıyor — aynı kamera farklı ekranda farklı img2img girdisi üretir. Düzeltme: snapshot öncesi `renderer.setSize(1440, 810, /*updateStyle=*/false)` + `cam.aspect=16/9` + `updateProjectionMatrix()`; snapshot sonrası ESKİ boyut/aspect birebir geri (fonksiyon zaten savedView/roof/gizmo restore deseni kullanıyor — aynı disipline uy).
**TUZAKLAR:** (1) `updateStyle=false` şart, yoksa canvas CSS bozulur. (2) Kamera yerleştirme `worldToPx` EXACT (px_delta=0) değişmezine dayanıyor — snapshot yolu ana render döngüsünün boyutlarını KALICI değiştirmemeli; restore'u `finally` benzeri kesinlikte yap. (3) Viewport 0×0 iken (ekran-arası) render çöker — boyut sıfırsa snapshot'ı reddet/ertele.
**Kabul:** Farklı pencere boyutlarında alınan snapshot'lar aynı piksel boyutunda; kamera yerleştirme UI'ı (kullanıcının sevdiği adım-4 akışı) birebir çalışır; `npm run build` sonrası prototipte doğrula.
**Efor:** ~1-2 saat.

### C3. Oda tipi fallback uyarısı

`io.js buildFloorplanMap` / `view3d.js colorFor`: tanınmayan oda tipi sessizce `_def` (bej) renge düşüyor → nano tipi yanlış okur. Export sırasında `_def`'e düşen oda oranı >%5 ise `console.warn` + export JSON'a `warnings:[]` alanı.
**Kabul:** Bozuk tipli planla uyarı tetiklenir; normal planda tetiklenmez.
**Efor:** ~30 dk.

### C4. Render regresyon fikstürü

Ana repo `mesken/` altına (gitignored alan) `tests_render/` klasörü: test7/8/9 iso girdileri + bilinen-iyi çıktılar + `check.py` — girdileri mevcut model sürümüne yeniden gönderir, çıktıyı referansla yan yana kaydeder (otomatik piksel diff'i BEKLEME — model nondeterministik olabilir; amaç insan-gözü kıyası için düzenli koşulabilir tek komut). `DURUM.md`'ye "model/prompt değişince bunu koş" notu ekle.
**Kabul:** `python tests_render/check.py` tek komutla kıyas görselleri üretir.
**Efor:** ~1-2 saat.

### C5. Mobilya render sinyalini tamamla (kalan boşluk: headless parity + prompt)

**DOĞRULANMIŞ mevcut durum (2026-07-02, kod kanıtlı — iş sanılandan KÜÇÜK):** Mobilya v2 zaten 3B mesh'in parçası (Mesken adım-2): `furnList` → `__furnitureGroup` mesh'leri (`view3d.js` ~1236); `buildFloorplanMap` oda başına `furniture[]` export ediyor (`io.js` ~502, 549-551, kaynak `window.__kptaFurniture`); kamera export'u koni içindeki mobilyayı `furniture_seen` olarak raporluyor (`io.js furnitureSeen` ~641); UI'da "Render'a mobilya ekle" checkbox'ı var (`view3d.js` ~165, boş listede `autoFurnishAll` ~559); `snapCameraDataURL` yalnız gizmo+etiket gizler → mobilya hem iso PNG snapshot'ında hem Opsiyon B kamera snapshot'larında GÖRÜNÜR.

**Kalan boşluklar (yapılacak iş bunlar):**
1. **Headless parity:** ana repo `mesken/00_CALISAN_RECETE/scripts/iso_render.py` (saf-PIL iso yolu) yalnız oda+duvar çiziyor — mobilya YOK (grep'le doğrulandı). `floorplan-map.json`'daki `furniture[]` alanından aynı sade kutuları çizdir (tip başına sabit koyu ton, etiketsiz; pozisyon MUTLAK metre, parametrik `__w/__d` boyutları akmalı — mobilya hafıza tuzakları). Hedef: tarayıcı iso PNG ile headless iso PNG aynı mobilya sinyalini taşısın.
2. **Prompt denetimi:** `render-server.js` `isoPrompt` + kamera reçetelerinde "furniture blocks are real furniture positions; dress them, do not move or invent" tarzı açık talimat var mı? Yoksa ekle.
3. **Kanıt:** test7-9 tarzı A/B kıyası (mobilyalı iso → nano, prompt satırı ile/olmadan) üret, hükmü `DURUM.md`'ye yaz.

**Kabul:** `iso_render.py` çıktısı mobilyalı planda kutuları gösterir ve tarayıcı iso PNG ile örtüşür; nano çıktısında mobilya konumları korunur (göz kıyası); mobilyasız planda iki yol da eskisi gibi.
**Efor:** ~yarım gün (headless parity ağırlıklı).

### C6. (STRETCH) Derinlik haritası export'u

`view3d.js`'e `snapCameraDepthMap(cam)` — aynı kamera açısından depth render (three.js depth material). Amaç: nano-banana deprecate olursa ControlNet-Depth (Flux vb.) kaçış yolu. Şimdilik sadece üretilebilir olsun; pipeline'a bağlama.
**Efor:** ~yarım gün. Zaman yoksa atla.

### C7. A/B varsayılanı (KULLANICIYA SOR — ürün kararı)

Şu an her kamera için A (prompt/txt2img) + B (snapshot/img2img) PARALEL koşuyor → 2× render maliyeti. Öneri: B varsayılan (sadık), A istek-üzerine (stil seçeneği). Kredi ekonomisine bağlanır (kredi muhasebesi ayrı oturum). **Uygulamadan önce kullanıcıya sor**; onaylarsa `render-server.js /camera` + prototip adım-5 UI'da varsayılanı değiştir, "ikisini de üret" seçenek olarak kalsın.

---

## 6) PAKET D — ML RANKER (AYRI OTURUM önerilir; bu bölüm o oturumun tohum brief'i)

**Karar bağlamı:** 41-örnek raster finetune EZBER çıktı (held-out Δ+0.013) ama `ml/phase2/region_signal.py` kullanıcı tercihinin **bölge-uzayında %82-92 yönsel tutarlı** olduğunu kanıtladı (dwelling_share ↑ %92, yatak ↑ %85, koridor ↓ %85, dikdörtgenlik ↑ %85). Sonuç: üretimi ML'e verme; **motor üretir, ML SIRALAR.**

**Mimari:**
1. **Aday üretimi (motor, JS):** `generate()`'e stokastik/parametrik varyasyon düğmeleri — koridor konum adayları, ayırıcı (cut) konum varyantları, daire sıralaması permütasyonları, rectify aç/kapa. Hedef: istek başına K=5-10 GEÇERLİ aday (mevzuat filtresi: `runChecks` bad sayısı artmayan). Headless harness mevcut: `ml/engine.js` (VM tabanlı) — aday üretimini oraya ekle.
2. **Öznitelik çıkarımı (Python):** aday başına ~10 bölge-agregat özniteliği — `region_signal.py`'deki HAZIR kod: dwelling_share, yatak/salon/koridor/circ_share, mean_rect, unit_area_cv, unit_area_spread (+ ekle: daire başına ihlal sayısı, antre oranı).
3. **Skorlayıcı:** XGBoost ya da 2-katman MLP; eğitim verisi = 39 eşli vaka (motor-orijinal < kullanıcı-düzeltilmiş) + pertürbasyon negatifleri (düzeltilmiş plandan rastgele bozulmuş K varyant). Pairwise ranking loss (ya da basit: corrected=1, original/perturbed=0 sınıflandırma).
4. **Değerlendirme:** held-out eşlerde "düzeltilmişi orijinalin üstüne sıralama" doğruluğu (hedef >%80 — region_signal yönsellikleri bunun mümkün olduğunu söylüyor). Ezber kontrolü: batch1'de eğit, batch2'de test (ve tersi).
5. **UI (KPTA, ayrı adım):** "Yerleşimi Oluştur" 3 varyant sunar (Plan A/B/C, skor sırasıyla); kullanıcının SEÇİMİ ücretsiz tercih etiketi olarak loglanır (self-training hafıza kararıyla uyumlu: kabul=zayıf pozitif, DEBUG indirmesi hariç). Skorlayıcı küçük — ileride JS'e port edilebilir (offline kısıtı korunur); ilk sürümde offline Python yeterli.

**İlkeler:** Mevzuat sert-kısıtları skora KARIŞMAZ (filtre olarak önce uygulanır — kullanıcının etiketleme felsefesi); skor sadece yasal adayları sıralar. Raster U-Net'e yeni yatırım YOK (ezber kanıtı); GNN/vektör-difüzyon L1-B vektör temsili gelene kadar BEKLER (L2 fazı) — ranker o güne kadar tercih verisini biriktirir.

**Efor:** 1-2 hafta (aday üretimi 2-3 gün, öznitelik+eğitim 2-3 gün, değerlendirme 1-2 gün, varyant UI 2-3 gün).
**İlk doğrulama hedefi:** held-out ranking doğruluğu raporu — UI'a bağlamadan ÖNCE kullanıcıya sonuç sun.

---

## 7) TUZAKLAR (hafıza + denetimden damıtılmış; dokunulan dosyalara göre)

- `generate()` elle düzenleri SIFIRLAR (bilinçli) — undo/geçmiş akışlarına dokunursan: cut undo **snapshot-temelli** (`restoreState`), `stateSnapshot` cuts'ı DEEP-COPY saklar (referans tuzağı yaşandı); `generate()` filtresi `'cut'||'ulayout'||'__snap'` kayıtlarını korur.
- `restoreState`'e onarım/ek mantık KOYMA — undo/redo/kat-geçişi onu kullanır (import onarımı bu yüzden `importPlanText`'te).
- `healDisconnected` koridoru BİLEREK atlar; çekirdek asla alıcı olmaz (`!isStructReg`); `regComponentCount` DELTA mantığı (2→2 geçer) — bu muhafızları "temizlik" diye bozma.
- Koridor genişliği `g.minSide` DEĞİL `corridorMinWidth` (bbox körlüğü vakası); benzer proxy-ölçüm hatasına yeni kod ekleme.
- Mobilya: MUTLAK-metre pozisyon, parametrik `__w/__d` her tüketicide akmalı, `furnDoorBlocked`, lazy `ensureFMAT`, `window.__kptaFurniture` kalıcılık.
- Kamera `room_id` TUTAMAÇ-odası çapalı (koni-dominant değil) — `cameraViewInfo` mantığını değiştirme.
- Testler `eval` tabanlı headless: üst-düzey `const` çakışmaları ve DOM varsayımları test kırar; yeni global eklerken `tests/support` stub'larını kontrol et.
- Emoji yasağı MUTLAK; `styles.css` global `svg{width:100%}` — inline SVG ikonlara inline boyut ver.
- Pre-commit hook build'i kendisi koşar ama YENİ dosyaları stage'lemez — yeni dosya eklediysen `git add` unutma; "commit & push" istenirse `npm run ship "mesaj"`.

---

## 8) DOĞRULAMA KOMUTLARI + KABUL ÖZETİ

```bash
npm test                         # strict paket — her alt-adım sonunda yeşil
npm run test:smoke               # dokunulan alana göre
node tests/snapshot-regression.js            # A1 sonrası: sıfır fark
node tests/snapshot-regression.js --write    # SADECE bilinçli taban güncellemesi
npm run build                    # tekdosya + mesken prototip tazelenir
node --check <dosya>.js          # şüpheli bozulmada ilk hamle
```

**Oturum-sonu kontrol listesi (her paket için):**
1. `npm test` yeşil + snapshot-regresyon birebir (davranış-koruyan işlerde).
2. `npm run build` başarılı; prototip görsel smoke (özellikle B ve C2 sonrası adım 3-4-5 akışı).
3. Commit(ler) atıldı; işin özeti + bilinçli takaslar (ör. B1 canlı-reflow → hayalet) kullanıcıya raporlandı.
4. Bu brief'te "KULLANICIYA SOR" işaretli maddeler (C7) sorulmadan uygulanMADI.
5. Guard-uyarı sayıları (A3) raporlandı — hangi senaryolarda onarım zinciri limit doluyor.
